diff --git a/.gitignore b/.gitignore
index 8f2edfd..da99623 100644
--- a/.gitignore
+++ b/.gitignore
@@ -8,3 +8,9 @@ node_modules/
 .DS_Store
 _github_push.html
 test_*.mjs
+# Tesseract OCR training data — downloaded at runtime
+eng.traineddata
+allahabad-hc-mcp/eng.traineddata
+api-server/eng.traineddata
+# Local test scripts
+allahabad-hc-mcp/ocr_test.mjs
diff --git a/LegalDesk.html b/LegalDesk.html
index 198ebc1..103fe5c 100644
--- a/LegalDesk.html
+++ b/LegalDesk.html
@@ -2080,7 +2080,7 @@ const AzureTranslator = {
       { code: 'ta', name: 'Tamil', nativeName: 'தமிழ்' },
       { code: 'te', name: 'Telugu', nativeName: 'తెలుగు' },
       { code: 'mr', name: 'Marathi', nativeName: 'मराठी' },
-      { code: 'gu', name: 'Gujarati', nativeName: 'ગુજરાતી' },
+      { code: 'gu', name: 'Gujarati', nativeName: 'ગુજરાতી' },
       { code: 'kn', name: 'Kannada', nativeName: 'ಕನ್ನಡ' },
       { code: 'ml', name: 'Malayalam', nativeName: 'മലയാളം' },
       { code: 'pa', name: 'Punjabi', nativeName: 'ਪੰਜਾਬੀ' },
@@ -2162,6 +2162,12 @@ const CourtAPI = {
     return this._fetch('/api/court-calendar');
   },
 
+  /** Fetch live Allahabad HC legal news (merged headlines + judgments RSS) */
+  async getNews(limit) {
+    const q = limit ? `?limit=${limit}` : '';
+    return this._fetch(`/api/news${q}`);
+  },
+
   /** Convert raw API case data to the app's Matter format */
   apiCaseToMatter(apiCase, index) {
     const id = 'live_' + (apiCase.caseType || 'X') + '_' + (apiCase.caseNo || index) + '_' + (apiCase.year || '');
@@ -2252,6 +2258,17 @@ const COURTS = [
   'Allahabad High Court (Lucknow Bench)',
 ];
 
+// Auto-detect bench from Advocate Roll Number prefix.
+// Convention at Allahabad HC: "A/..." = Principal Seat (Allahabad), "B/..." = Lucknow Bench.
+// Returns the matching court string from COURTS, or null if we can't tell.
+function suggestCourtFromRoll(rollNumber) {
+  if (!rollNumber) return null;
+  const s = String(rollNumber).trim().toUpperCase();
+  if (/^B\//.test(s)) return COURTS[1]; // Lucknow Bench
+  if (/^A\//.test(s)) return COURTS[0]; // Principal Seat
+  return null;
+}
+
 // ─────────────────────────────────────────────────────────────────────
 // CASE TYPE MAPPING - Allahabad High Court
 // ─────────────────────────────────────────────────────────────────────
@@ -2677,14 +2694,217 @@ const DEFAULT_INVOICES = [
   { id: 'inv4', subject: 'Retainer Fee - May 2026', total: 50000, date: '2026-05-01', matterId: null },
 ];
 
-const LEGAL_NEWS = [
-  { id: 'n1', title: 'Supreme Court upholds constitutional validity of new criminal laws', desc: 'The apex court dismissed a batch of petitions challenging BNS, BNSS and BSA.', time: '2 hours ago', url: 'https://main.sci.gov.in/supremecourt/judgements.htm' },
-  { id: 'n2', title: 'Allahabad HC issues guidelines on bail applications in POCSO cases', desc: 'New guidelines mandate consideration of specific factors.', time: '5 hours ago', url: 'https://www.allahabadhighcourt.in/jo.htm' },
-  { id: 'n3', title: 'E-filing mandatory for all High Courts from July 2026', desc: 'Supreme Court directed all High Courts to implement e-filing.', time: '1 day ago', url: 'https://www.allahabadhighcourt.in/efiling_21042021_landing.html' },
-  { id: 'n4', title: 'Bar Council of India announces revised rules for advocate registration', desc: 'Updated rules for enrollment and renewal of advocate licenses.', time: '2 days ago', url: 'https://www.barcouncilofindia.org' },
-  { id: 'n5', title: 'Lucknow Bench to start evening courts for commercial disputes', desc: 'Pilot program beginning June 2026 for expedited dispute resolution.', time: '3 days ago', url: 'https://www.allahabadhighcourt.in/calendar/itemWiseList.jsp?group=5' },
+// Fallback only — shown when the live RSS fetch is loading or has failed.
+// Real news comes from useLegalNews() → /api/news (Allahabad HC RSS feeds).
+const LEGAL_NEWS_FALLBACK = [
+  { id: 'n1', title: 'Loading latest legal updates from Allahabad High Court…', desc: 'Live RSS feeds (headlines + AFR/Landmark judgments) are being fetched. If this message persists, the backend may be offline.', time: 'just now', url: 'https://www.allahabadhighcourt.in/' },
 ];
 
+// Format an ISO date as "x hours ago" / "x days ago" for the news card.
+function relativeTime(iso) {
+  if (!iso) return '';
+  const t = Date.parse(iso);
+  if (isNaN(t)) return '';
+  const diff = Date.now() - t;
+  const min = Math.floor(diff / 60000);
+  if (min < 1) return 'just now';
+  if (min < 60) return `${min} min ago`;
+  const hr = Math.floor(min / 60);
+  if (hr < 24) return `${hr} hour${hr === 1 ? '' : 's'} ago`;
+  const d = Math.floor(hr / 24);
+  if (d < 30) return `${d} day${d === 1 ? '' : 's'} ago`;
+  const mo = Math.floor(d / 30);
+  if (mo < 12) return `${mo} month${mo === 1 ? '' : 's'} ago`;
+  return new Date(iso).toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' });
+}
+
+// Module-level cache so dashboard + Legal Updates page share one network call.
+let _legalNewsCache = null;
+let _legalNewsPromise = null;
+function fetchLegalNewsOnce() {
+  if (_legalNewsCache) return Promise.resolve(_legalNewsCache);
+  if (_legalNewsPromise) return _legalNewsPromise;
+  _legalNewsPromise = CourtAPI.getNews(40)
+    .then(res => {
+      if (res && res.status === 'success' && Array.isArray(res.items)) {
+        _legalNewsCache = res.items.map(it => ({
+          id: it.id,
+          title: it.title,
+          desc: it.description || '',
+          time: relativeTime(it.pubDate) || (it.pubDateRaw || ''),
+          url: it.link || '#',
+          source: it.source, // 'headlines' | 'judgments'
+        }));
+        return _legalNewsCache;
+      }
+      return LEGAL_NEWS_FALLBACK;
+    })
+    .catch(() => LEGAL_NEWS_FALLBACK)
+    .finally(() => { _legalNewsPromise = null; });
+  return _legalNewsPromise;
+}
+
+// React hook for components that render legal news.
+function useLegalNews() {
+  const [items, setItems] = useState(_legalNewsCache || LEGAL_NEWS_FALLBACK);
+  const [loading, setLoading] = useState(!_legalNewsCache);
+  useEffect(() => {
+    let alive = true;
+    fetchLegalNewsOnce().then(list => {
+      if (!alive) return;
+      setItems(list);
+      setLoading(false);
+    });
+    return () => { alive = false; };
+  }, []);
+  return { items, loading };
+}
+
+// Live Court View — replaces the unworkable iframe of courtview2.allahabadhighcourt.in.
+// The court page sends X-Frame-Options: SAMEORIGIN, so iframe embedding is blocked by every browser.
+// Instead we fetch /api/court-view (which scrapes the same page server-side) and render natively.
+function LiveCourtViewPanel({ bench, externalUrl, refreshSignal }) {
+  const [data, setData] = useState(null);
+  const [loading, setLoading] = useState(false);
+  const [error, setError] = useState(null);
+  const [refreshedAt, setRefreshedAt] = useState(null);
+
+  useEffect(() => {
+    let alive = true;
+    let timer = null;
+    const load = async (showSpinner) => {
+      if (showSpinner) setLoading(true);
+      try {
+        const res = await CourtAPI.getCourtView(bench);
+        if (!alive) return;
+        setData(res);
+        setError(null);
+        setRefreshedAt(new Date());
+      } catch (e) {
+        if (!alive) return;
+        setError(e.message || 'Failed to fetch court view');
+      } finally {
+        if (alive) setLoading(false);
+      }
+    };
+    load(true);
+    timer = setInterval(() => load(false), 30000); // backend caches for 30s
+    return () => { alive = false; if (timer) clearInterval(timer); };
+  }, [bench, refreshSignal]);
+
+  const courts = (data && data.courts) || [];
+  const inSession = courts.filter(c => c.status === 'in_session');
+  const notInSession = courts.filter(c => c.status !== 'in_session');
+
+  return (
+    <div style={{padding:'12px 16px'}}>
+      <div style={{display:'flex', justifyContent:'space-between', alignItems:'center', marginBottom:10, flexWrap:'wrap', gap:8}}>
+        <div style={{fontSize:12, color:'var(--text-muted)'}}>
+          {loading && !data && 'Loading live court view…'}
+          {data && (
+            <>
+              <strong style={{color:'var(--text-primary)'}}>{courts.length}</strong> courts ·
+              <strong style={{color:'var(--success)', marginLeft:6}}>{inSession.length}</strong> in session ·
+              <strong style={{color:'var(--text-muted)', marginLeft:6}}>{notInSession.length}</strong> idle
+              {refreshedAt && <span style={{marginLeft:10, fontFamily:'monospace', fontSize:11}}>· refreshed {refreshedAt.toLocaleTimeString('en-IN', { hour12: false })}</span>}
+            </>
+          )}
+        </div>
+        <div style={{display:'flex', gap:8}}>
+          <a href={externalUrl} target="_blank" rel="noopener" style={{fontSize:11, color:'var(--primary)', textDecoration:'none'}}>Open official page ↗</a>
+        </div>
+      </div>
+
+      {error && (
+        <div style={{padding:'10px 14px', borderRadius:'var(--radius)', background:'var(--danger-bg)', color:'var(--danger)', fontSize:13, marginBottom:10}}>
+          Couldn't reach the live court view backend: {error}
+        </div>
+      )}
+
+      {inSession.length > 0 && (
+        <div style={{overflowX:'auto', border:'1px solid var(--border)', borderRadius:'var(--radius)'}}>
+          <table style={{width:'100%', borderCollapse:'collapse', fontSize:12}}>
+            <thead>
+              <tr style={{background:'var(--bg-elevated)', textAlign:'left'}}>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Court</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Item</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Case</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Title</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Petitioner's Counsel</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Respondent's Counsel</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Progress</th>
+              </tr>
+            </thead>
+            <tbody>
+              {inSession.map((c, i) => (
+                <tr key={i} style={{borderBottom:'1px solid var(--border-light)', background: i % 2 ? 'var(--bg-card)' : 'transparent'}}>
+                  <td style={{padding:'8px 10px', fontWeight:700, color:'var(--primary)'}}>{c.courtNo}</td>
+                  <td style={{padding:'8px 10px', fontFamily:'monospace'}}>{c.currentItem}</td>
+                  <td style={{padding:'8px 10px', fontFamily:'monospace', whiteSpace:'nowrap'}}>{c.caseRef || c.caseBeingHeard || '—'}</td>
+                  <td style={{padding:'8px 10px', maxWidth:280}}>{c.title || '—'}</td>
+                  <td style={{padding:'8px 10px', maxWidth:200, fontSize:11, color:'var(--text-secondary)'}}>{c.petitionerCounsel || '—'}</td>
+                  <td style={{padding:'8px 10px', maxWidth:200, fontSize:11, color:'var(--text-secondary)'}}>{c.respondentCounsel || '—'}</td>
+                  <td style={{padding:'8px 10px', fontSize:11}}>{c.progress || c.causelistType || ''}</td>
+                </tr>
+              ))}
+            </tbody>
+          </table>
+        </div>
+      )}
+
+      {data && inSession.length === 0 && (
+        <div style={{padding:'30px 20px', textAlign:'center', background:'var(--bg-card)', border:'1px dashed var(--border)', borderRadius:'var(--radius)'}}>
+          <div style={{fontSize:36, marginBottom:8, opacity:0.5}}>{Icons.monitor}</div>
+          <div style={{fontWeight:700, marginBottom:4}}>No courts in session right now</div>
+          <div style={{fontSize:12, color:'var(--text-muted)', maxWidth:420, margin:'0 auto'}}>
+            All {courts.length} court rooms show "not in session". This is normal outside hearing hours, on weekends, holidays, or during court vacations. Data refreshes every 30 seconds.
+          </div>
+        </div>
+      )}
+
+      {data && notInSession.length > 0 && (
+        <details style={{marginTop:12}}>
+          <summary style={{cursor:'pointer', fontSize:12, color:'var(--text-muted)'}}>Show {notInSession.length} idle court room{notInSession.length === 1 ? '' : 's'}</summary>
+          <div style={{display:'flex', flexWrap:'wrap', gap:6, marginTop:8}}>
+            {notInSession.map((c, i) => (
+              <span key={i} style={{padding:'3px 8px', fontSize:11, background:'var(--bg-elevated)', border:'1px solid var(--border)', borderRadius:'var(--radius-full)', color:'var(--text-muted)'}}>
+                {c.courtNo}
+              </span>
+            ))}
+          </div>
+        </details>
+      )}
+    </div>
+  );
+}
+
+// Compact Legal Updates card used on the dashboard.
+function LegalUpdatesCard({ navigate, limit = 4 }) {
+  const { items, loading } = useLegalNews();
+  return (
+    <div className="card">
+      <div className="card-header">
+        <span className="card-title">Legal Updates</span>
+        <button className="btn btn-sm btn-ghost" onClick={()=>navigate('legalupdates')}>View All</button>
+      </div>
+      <div className="card-body" style={{padding:'8px 20px'}}>
+        {loading && items === LEGAL_NEWS_FALLBACK && (
+          <div style={{fontSize:12, color:'var(--text-muted)', padding:8, textAlign:'center'}}>Loading live updates…</div>
+        )}
+        {items.slice(0, limit).map(n => (
+          <div key={n.id} className="news-card" style={{cursor: n.url && n.url !== '#' ? 'pointer' : 'default'}} onClick={() => { if (n.url && n.url !== '#') window.open(n.url, '_blank', 'noopener'); }}>
+            <div className="news-thumb">{Icons.newspaper}</div>
+            <div className="news-content">
+              <div className="news-title">{n.title}</div>
+              <div className="news-time">{n.time}{n.source ? ` · ${n.source === 'judgments' ? 'Judgment' : 'Headline'}` : ''}</div>
+            </div>
+          </div>
+        ))}
+      </div>
+    </div>
+  );
+}
+
 const COURT_RESOURCES = {
   judgments: {
     title: 'Judgments & Orders',
@@ -2983,7 +3203,7 @@ function LoginPage({ onLogin }) {
         // Also check localStorage for a cached profile (current session or saved from previous logout)
         const cachedUser = LS.get('user') || LS.get('profile_' + user.uid);
         if ((profile && profile.verified) || (cachedUser && cachedUser.verified && cachedUser.uid === user.uid)) {
-          // Returning user — skip verification
+          // Returning user — skip verification, go straight to dashboard
           const mergedProfile = { ...(cachedUser || {}), ...(profile || {}) };
           onLogin({
             ...mergedProfile,
@@ -3028,6 +3248,7 @@ function LoginPage({ onLogin }) {
       if (user) {
         setGoogleUser(user);
         setForm(prev => ({ ...prev, name: user.name }));
+        // Check if returning user
         let profile = null;
         try {
           const profilePromise = FirestoreService.getProfile(user.uid);
@@ -3300,10 +3521,32 @@ function LoginPage({ onLogin }) {
               </div>
               <div className="form-group">
                 <label className="form-label">Advocate ID / AOR No. <span style={{fontSize:11, color:'var(--text-muted)', fontWeight:400}}>(used for case lookup)</span></label>
-                <input className="form-input" placeholder="e.g. B/A2401/2019 or AOR/5678" value={form.rollNo} onChange={e => setForm({...form, rollNo: e.target.value})} />
+                <input className="form-input" placeholder="e.g. B/A2401/2019 or AOR/5678" value={form.rollNo} onChange={e => {
+                  const rollNo = e.target.value;
+                  // Auto-suggest the bench from the roll prefix (A/ = Allahabad, B/ = Lucknow Bench).
+                  const suggested = suggestCourtFromRoll(rollNo);
+                  setForm(prev => ({
+                    ...prev,
+                    rollNo,
+                    // Only override the court if the user hasn't manually changed it yet,
+                    // or if their current value is the default.
+                    court: suggested && (prev.court === COURTS[0] || !prev.court) ? suggested : prev.court,
+                  }));
+                }} />
+                {form.rollNo && (() => {
+                  const s = suggestCourtFromRoll(form.rollNo);
+                  if (s && s !== form.court) {
+                    return <div style={{fontSize:11, color:'var(--primary)', marginTop:4}}>
+                      Hint: roll numbers starting with {form.rollNo.trim()[0].toUpperCase()}/ are typically <strong>{s}</strong>.
+                      <button type="button" style={{marginLeft:6, fontSize:11, padding:'2px 8px', border:'1px solid var(--primary)', background:'transparent', color:'var(--primary)', borderRadius:4, cursor:'pointer'}}
+                        onClick={() => setForm(prev => ({...prev, court: s}))}>Use {s.includes('Lucknow') ? 'Lucknow Bench' : 'Principal Seat'}</button>
+                    </div>;
+                  }
+                  return null;
+                })()}
               </div>
               <div className="form-group">
-                <label className="form-label">Select Court</label>
+                <label className="form-label">Select Court / Bench <span style={{fontSize:11, color:'var(--text-muted)', fontWeight:400}}>(determines which bench is queried first)</span></label>
                 <select className="form-select" value={form.court} onChange={e => setForm({...form, court: e.target.value})}>
                   {COURTS.map(c => <option key={c} value={c}>{c}</option>)}
                 </select>
@@ -3678,8 +3921,9 @@ function DashboardPage() {
     },
     causelists: {
       label: 'Cause Lists', labelHi: 'वाद सूची', icon: Icons.fileText,
-      urlAld: 'https://www.allahabadhighcourt.in/causelist/causelist_ald.htm',
-      urlLko: 'https://www.allahabadhighcourt.in/causelist/causelist_lko.htm',
+      // The old /causelist/causelist_*.htm URLs returned 404. Live cause list now lives on the CCMS app.
+      urlAld: 'https://www.allahabadhighcourt.in/apps/status_ccms/index.php/causelist',
+      urlLko: 'https://hclko.allahabadhighcourt.in/status/index.php/cause-list',
       desc: "Today's cause list — cases scheduled for hearing",
     },
     efiling: {
@@ -3828,20 +4072,26 @@ function DashboardPage() {
             <span>{currentPanel.desc} — {liveBench === 'lucknow' ? 'Lucknow Bench' : 'Allahabad (Principal Seat)'}</span>
             <span style={{fontFamily:'monospace', fontSize:10, opacity:0.6}}>{currentUrl.replace('https://',''). split('/').slice(0,2).join('/')}</span>
           </div>
-          <div style={{padding:'24px 20px', textAlign:'center', minHeight:200, display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', gap:16, background:'var(--bg-card)'}}>
-            <div style={{width:64, height:64, borderRadius:'50%', background:'var(--primary-bg)', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--primary)'}}>{currentPanel.icon}</div>
-            <div>
-              <div style={{fontWeight:700, fontSize:16, marginBottom:4}}>{currentPanel.label}</div>
-              <div style={{fontSize:13, color:'var(--text-secondary)', maxWidth:400, margin:'0 auto'}}>{currentPanel.desc}</div>
-            </div>
-            <div style={{display:'flex', gap:8, flexWrap:'wrap', justifyContent:'center'}}>
-              <button className="btn btn-primary" onClick={() => window.open(currentUrl, '_blank')}>{Icons.externalLink} Open {currentPanel.label}</button>
-              <button className="btn btn-outline" onClick={() => openCourtPopup(currentUrl, currentPanel.label)}>{Icons.monitor} Open in Popup Window</button>
-            </div>
-            <div style={{fontSize:11, color:'var(--text-muted)', marginTop:8, maxWidth:450}}>
-              Court websites restrict embedding for security. Click the button above to open the live {currentPanel.label.toLowerCase()} directly from the Allahabad High Court website.
+          {livePanel === 'courtview' ? (
+            // Native live render — replaces the unworkable iframe.
+            // /api/court-view scrapes courtview2.allahabadhighcourt.in server-side and returns JSON.
+            <LiveCourtViewPanel bench={liveBench} externalUrl={currentUrl} refreshSignal={iframeKey} />
+          ) : (
+            <div style={{padding:'24px 20px', textAlign:'center', minHeight:200, display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', gap:16, background:'var(--bg-card)'}}>
+              <div style={{width:64, height:64, borderRadius:'50%', background:'var(--primary-bg)', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--primary)'}}>{currentPanel.icon}</div>
+              <div>
+                <div style={{fontWeight:700, fontSize:16, marginBottom:4}}>{currentPanel.label}</div>
+                <div style={{fontSize:13, color:'var(--text-secondary)', maxWidth:400, margin:'0 auto'}}>{currentPanel.desc}</div>
+              </div>
+              <div style={{display:'flex', gap:8, flexWrap:'wrap', justifyContent:'center'}}>
+                <button className="btn btn-primary" onClick={() => window.open(currentUrl, '_blank')}>{Icons.externalLink} Open {currentPanel.label}</button>
+                <button className="btn btn-outline" onClick={() => openCourtPopup(currentUrl, currentPanel.label)}>{Icons.monitor} Open in Popup Window</button>
+              </div>
+              <div style={{fontSize:11, color:'var(--text-muted)', marginTop:8, maxWidth:450}}>
+                Court websites restrict embedding for security. Click the button above to open the live {currentPanel.label.toLowerCase()} directly from the Allahabad High Court website.
+              </div>
             </div>
-          </div>
+          )}
         </div>
       </div>
 
@@ -3962,24 +4212,8 @@ function DashboardPage() {
           </div>
         </div>
         <div>
-          {/* Legal Updates */}
-          <div className="card">
-            <div className="card-header">
-              <span className="card-title">Legal Updates</span>
-              <button className="btn btn-sm btn-ghost" onClick={()=>navigate('legalupdates')}>View All</button>
-            </div>
-            <div className="card-body" style={{padding:'8px 20px'}}>
-              {LEGAL_NEWS.slice(0,4).map(n => (
-                <div key={n.id} className="news-card">
-                  <div className="news-thumb">{Icons.newspaper}</div>
-                  <div className="news-content">
-                    <div className="news-title">{n.title}</div>
-                    <div className="news-time">{n.time}</div>
-                  </div>
-                </div>
-              ))}
-            </div>
-          </div>
+          {/* Legal Updates — live from Allahabad HC RSS feeds (was previously a hardcoded 5-item array) */}
+          <LegalUpdatesCard navigate={navigate} limit={4} />
 
           {/* Quick Actions */}
           <div className="card" style={{marginTop:16}}>
@@ -4035,6 +4269,74 @@ function DashboardPage() {
 // ─────────────────────────────────────────────────────────────────────
 // MATTERS PAGE
 // ─────────────────────────────────────────────────────────────────────
+// Banner explaining the live-fetch state on the Matters page.
+// Replaces the silent display of demo matters with a clear message about what's happening.
+function LiveFetchBanner() {
+  const { liveFetchStatus, loadingLiveCases, user, matters } = useContext(AppContext);
+  if (!user) return null;
+  // Detect whether the visible matters are the canned demo set (Rajesh Kumar Mishra etc.)
+  const isDemoData = matters?.length > 0 && matters[0]?.id === 'm1' && matters[0]?.title?.includes('Rajesh Kumar Mishra');
+  const status = liveFetchStatus || { kind: 'idle' };
+
+  if (isDemoData && (user.barCouncil || user.rollNo)) {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--warning-bg)', color:'var(--warning)', border:'1px solid var(--warning)', fontSize:13, display:'flex', gap:10, alignItems:'flex-start'}}>
+        <div style={{fontWeight:700}}>⚠ Sample data shown.</div>
+        <div>
+          You're seeing 3 demo matters (Rajesh Kumar Mishra etc.), not your real cases.
+          {loadingLiveCases ? ' Fetching your live matters now…' : ' Live fetch in progress — refresh the page in a moment.'}
+        </div>
+      </div>
+    );
+  }
+
+  if (status.kind === 'loading' || loadingLiveCases) {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--primary-bg)', color:'var(--primary)', fontSize:13}}>
+        Fetching your live matters from Allahabad HC CCMS… The court portal uses CAPTCHAs and can take 30-60 seconds per bench on the first call.
+      </div>
+    );
+  }
+  if (status.kind === 'upstream_maintenance') {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--warning-bg)', color:'var(--warning)', border:'1px solid var(--warning)', fontSize:13}}>
+        <strong>Allahabad HC CCMS (Lucknow Bench) is in maintenance.</strong>{' '}
+        {status.message}
+      </div>
+    );
+  }
+  if (status.kind === 'no_cases') {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--info-bg)', color:'var(--info)', border:'1px solid var(--info)', fontSize:13}}>
+        <strong>No live cases found</strong> for roll number {status.lookupId ? <code>{status.lookupId}</code> : ''}. {status.message}
+        {status.benchStatus && (
+          <div style={{fontSize:11, marginTop:6, opacity:0.85}}>
+            Allahabad: {status.benchStatus.allahabad?.status}{status.benchStatus.allahabad?.count > 0 ? ` (${status.benchStatus.allahabad.count})` : ''} ·
+            Lucknow: {status.benchStatus.lucknow?.status}{status.benchStatus.lucknow?.count > 0 ? ` (${status.benchStatus.lucknow.count})` : ''}
+          </div>
+        )}
+      </div>
+    );
+  }
+  if (status.kind === 'backend_down') {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--danger-bg)', color:'var(--danger)', border:'1px solid var(--danger)', fontSize:13}}>
+        Backend API at <code>legaldesk-api.onrender.com</code> is unreachable. Free Render tier sleeps after 15 min — first request can take 30s. Wait, then refresh.
+      </div>
+    );
+  }
+  if (status.kind === 'success' && status.benchStatus) {
+    return (
+      <div style={{padding:'10px 14px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--success-bg)', color:'var(--success)', fontSize:12}}>
+        Live: <strong>{status.allahabadCount}</strong> Allahabad ·
+        <strong style={{marginLeft:6}}>{status.lucknowCount}</strong> Lucknow Bench
+        {status.benchStatus.lucknow?.note && <div style={{fontSize:11, opacity:0.85, marginTop:4}}>{status.benchStatus.lucknow.note}</div>}
+      </div>
+    );
+  }
+  return null;
+}
+
 function MattersPage() {
   const { matters, setMatters, navigate, setSelectedMatter, addToast } = useContext(AppContext);
   const [tab, setTab] = useState('pending');
@@ -4091,6 +4393,8 @@ function MattersPage() {
         <button className="btn btn-primary" onClick={()=>setShowModal(true)}>{Icons.plus} Create New</button>
       </div>
 
+      <LiveFetchBanner />
+
       <div className="card">
         <div className="card-header" style={{flexWrap:'wrap'}}>
           <div className="tabs">
@@ -5803,7 +6107,10 @@ function DisplayBoardPage() {
                     {Icons.externalLink} Open Live Display Board
                   </button>
                   <button className="btn btn-outline" onClick={() => {
-                    openCourtPopup('https://www.allahabadhighcourt.in/causelist/causelist_' + (liveBench==='lucknow'?'lko':'ald') + '.htm', 'CauseList');
+                    const causelistUrl = liveBench==='lucknow'
+                      ? 'https://hclko.allahabadhighcourt.in/status/index.php/cause-list'
+                      : 'https://www.allahabadhighcourt.in/apps/status_ccms/index.php/causelist';
+                    openCourtPopup(causelistUrl, 'CauseList');
                     addToast('Cause List page opened', 'info');
                   }}>
                     {Icons.fileText} Today's Cause List
@@ -6001,14 +6308,28 @@ function DisplayBoardPage() {
 // LEGAL UPDATES PAGE
 // ─────────────────────────────────────────────────────────────────────
 function LegalUpdatesPage() {
+  const { items, loading } = useLegalNews();
+  const [filter, setFilter] = useState('all'); // all | headlines | judgments
+  const filtered = filter === 'all' ? items : items.filter(n => n.source === filter);
   return (
     <div className="content">
-      <div className="page-header"><h2 className="page-title">Legal Updates</h2></div>
+      <div className="page-header">
+        <h2 className="page-title">Legal Updates</h2>
+        <div style={{display:'flex', gap:8}}>
+          <button className={`btn btn-sm ${filter==='all'?'btn-primary':'btn-outline'}`} onClick={()=>setFilter('all')}>All ({items.length})</button>
+          <button className={`btn btn-sm ${filter==='headlines'?'btn-primary':'btn-outline'}`} onClick={()=>setFilter('headlines')}>Headlines</button>
+          <button className={`btn btn-sm ${filter==='judgments'?'btn-primary':'btn-outline'}`} onClick={()=>setFilter('judgments')}>Judgments</button>
+        </div>
+      </div>
+      {loading && items === LEGAL_NEWS_FALLBACK && (
+        <div style={{padding:20, color:'var(--text-muted)'}}>Loading live updates from Allahabad HC RSS feeds…</div>
+      )}
       <div style={{display:'grid', gridTemplateColumns:'repeat(auto-fill, minmax(340px, 1fr))', gap:16}}>
-        {LEGAL_NEWS.map(n => (
-          <div key={n.id} className="card" style={{cursor:'pointer', transition:'var(--transition)'}} onClick={() => window.open(n.url, '_blank', 'noopener')}>
-            <div style={{height:140, background:'var(--primary-bg)', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--primary)'}}>
+        {filtered.map(n => (
+          <div key={n.id} className="card" style={{cursor: n.url && n.url !== '#' ? 'pointer' : 'default', transition:'var(--transition)'}} onClick={() => { if (n.url && n.url !== '#') window.open(n.url, '_blank', 'noopener'); }}>
+            <div style={{height:140, background:'var(--primary-bg)', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--primary)', position:'relative'}}>
               <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><path d="M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 1-2 2Zm0 0a2 2 0 0 1-2-2v-9c0-1.1.9-2 2-2h2"/><path d="M18 14h-8M15 18h-5M10 6h8v4h-8V6Z"/></svg>
+              {n.source && <span style={{position:'absolute', top:8, right:8, background:'rgba(255,255,255,0.9)', color:'var(--primary)', fontSize:10, fontWeight:700, padding:'2px 8px', borderRadius:10, textTransform:'uppercase'}}>{n.source === 'judgments' ? 'Judgment' : 'Headline'}</span>}
             </div>
             <div className="card-body">
               <div style={{fontWeight:700, fontSize:15, marginBottom:6, lineHeight:1.4}}>{n.title}</div>
@@ -6707,6 +7028,41 @@ function SettingsPage() {
   const { user, setUser, theme, setTheme, addToast, teamMembers, setTeamMembers } = useContext(AppContext);
   const [newMemberName, setNewMemberName] = useState('');
   const [newMemberPhone, setNewMemberPhone] = useState('');
+  // Editable bench/court — defaults to the suggested bench based on the roll number prefix.
+  const suggestedCourt = suggestCourtFromRoll(user?.rollNo);
+  const [editingCourt, setEditingCourt] = useState(false);
+  const [chosenCourt, setChosenCourt] = useState(user?.court || suggestedCourt || COURTS[0]);
+  // advmgmtsys creds stored ONLY in localStorage (not in our DB; not transmitted to backend
+  // until the user explicitly clicks Connect). When implemented in a later sprint, the backend
+  // will use these creds once per fetch and discard them.
+  const [advmgmtCreds, setAdvmgmtCreds] = useState(() => LS.get('advmgmtsys_creds', { username: user?.rollNo || '', password: '' }));
+  const [showAdvmgmtForm, setShowAdvmgmtForm] = useState(false);
+
+  const saveCourt = () => {
+    const updated = { ...user, court: chosenCourt };
+    setUser(updated);
+    LS.set('user', updated);
+    if (user?.uid && typeof FirestoreService !== 'undefined' && FirestoreService.saveProfile) {
+      FirestoreService.saveProfile(user.uid, updated).catch(() => {});
+    }
+    setEditingCourt(false);
+    addToast('Court / bench updated', 'success');
+  };
+
+  const saveAdvmgmtCreds = () => {
+    if (!advmgmtCreds.username.trim() || !advmgmtCreds.password.trim()) {
+      addToast('Enter both username and password', 'error'); return;
+    }
+    LS.set('advmgmtsys_creds', advmgmtCreds);
+    setShowAdvmgmtForm(false);
+    addToast('Credentials saved locally. (Backend sync not yet wired — coming next sprint.)', 'info');
+  };
+
+  const clearAdvmgmtCreds = () => {
+    LS.remove('advmgmtsys_creds');
+    setAdvmgmtCreds({ username: user?.rollNo || '', password: '' });
+    addToast('Advmgmtsys credentials cleared from this browser', 'info');
+  };
 
   const handleLogout = async () => {
     // Cache profile for quick re-login (skip verification on next sign-in)
@@ -6766,8 +7122,72 @@ function SettingsPage() {
             <input className="form-input" value={user?.rollNo || ''} readOnly />
           </div>
           <div className="form-group">
-            <label className="form-label">Court</label>
-            <input className="form-input" value={user?.court || ''} readOnly />
+            <label className="form-label">Court / Bench</label>
+            {!editingCourt ? (
+              <div style={{display:'flex', gap:8, alignItems:'center'}}>
+                <input className="form-input" value={user?.court || ''} readOnly style={{flex:1}} />
+                <button type="button" className="btn btn-outline btn-sm" onClick={() => setEditingCourt(true)}>Change</button>
+              </div>
+            ) : (
+              <div style={{display:'flex', gap:8, alignItems:'center'}}>
+                <select className="form-select" value={chosenCourt} onChange={e => setChosenCourt(e.target.value)} style={{flex:1}}>
+                  {COURTS.map(c => <option key={c} value={c}>{c}</option>)}
+                </select>
+                <button type="button" className="btn btn-primary btn-sm" onClick={saveCourt}>Save</button>
+                <button type="button" className="btn btn-ghost btn-sm" onClick={() => { setChosenCourt(user?.court || COURTS[0]); setEditingCourt(false); }}>Cancel</button>
+              </div>
+            )}
+            {suggestedCourt && suggestedCourt !== user?.court && (
+              <div style={{fontSize:11, color:'var(--warning)', marginTop:6}}>
+                Heads up: your roll number <code>{user?.rollNo}</code> starts with <strong>{user?.rollNo?.[0]?.toUpperCase()}/</strong> which usually means <strong>{suggestedCourt}</strong>.
+                {!editingCourt && (
+                  <button type="button" style={{marginLeft:6, fontSize:11, padding:'2px 8px', border:'1px solid var(--warning)', background:'transparent', color:'var(--warning)', borderRadius:4, cursor:'pointer'}}
+                    onClick={() => { setChosenCourt(suggestedCourt); setEditingCourt(true); }}>Switch to {suggestedCourt.includes('Lucknow') ? 'Lucknow Bench' : 'Principal Seat'}</button>
+                )}
+              </div>
+            )}
+          </div>
+
+          {/* ─── CCMS / advmgmtsys credential connection (alpha) ─── */}
+          <div style={{borderTop:'1px solid var(--border)', paddingTop:20, marginTop:20}}>
+            <h3 style={{fontSize:15, fontWeight:700, marginBottom:8}}>{Icons.lock || '🔒'} Court Portal Connection (Alpha)</h3>
+            <p style={{fontSize:12, color:'var(--text-secondary)', marginBottom:12, lineHeight:1.55}}>
+              Public CCMS gives only listed-today / per-year case lookups. To pull your <strong>full case history</strong>{' '}
+              (the way MyMunshi does), LegalDesk needs your <strong>Allahabad HC Advocate Management System (advmgmtsys)</strong> login.
+              Register at <a href="https://hclko.allahabadhighcourt.in/advmgmtsys/registration" target="_blank" rel="noopener" style={{color:'var(--primary)'}}>advmgmtsys/registration</a> if you haven't already.
+            </p>
+            <p style={{fontSize:11, color:'var(--text-muted)', marginBottom:12, lineHeight:1.55}}>
+              <strong>Privacy:</strong> Your password is stored only in <em>this browser</em>. The backend uses it once per scheduled fetch and never persists it.
+              The end-to-end fetcher ships next sprint; for now you can pre-fill your username so it's ready to go.
+            </p>
+            {!showAdvmgmtForm && !LS.get('advmgmtsys_creds') && (
+              <button type="button" className="btn btn-outline btn-sm" onClick={() => setShowAdvmgmtForm(true)}>
+                {Icons.link || '🔗'} Connect CCMS account
+              </button>
+            )}
+            {!showAdvmgmtForm && LS.get('advmgmtsys_creds') && (
+              <div style={{display:'flex', gap:8, alignItems:'center'}}>
+                <span style={{fontSize:12, color:'var(--success)'}}>✓ Credentials saved for <code>{LS.get('advmgmtsys_creds')?.username}</code></span>
+                <button type="button" className="btn btn-ghost btn-sm" onClick={() => setShowAdvmgmtForm(true)} style={{marginLeft:'auto'}}>Edit</button>
+                <button type="button" className="btn btn-ghost btn-sm" style={{color:'var(--danger)'}} onClick={clearAdvmgmtCreds}>Clear</button>
+              </div>
+            )}
+            {showAdvmgmtForm && (
+              <div style={{background:'var(--bg-input)', padding:14, borderRadius:'var(--radius)', marginTop:8}}>
+                <div className="form-group" style={{marginBottom:10}}>
+                  <label className="form-label">Username (Advocate Roll, e.g. B/A2401/2019)</label>
+                  <input className="form-input" value={advmgmtCreds.username} onChange={e => setAdvmgmtCreds({...advmgmtCreds, username: e.target.value})} placeholder="B/A2401/2019" />
+                </div>
+                <div className="form-group" style={{marginBottom:10}}>
+                  <label className="form-label">Password</label>
+                  <input className="form-input" type="password" value={advmgmtCreds.password} onChange={e => setAdvmgmtCreds({...advmgmtCreds, password: e.target.value})} placeholder="Your advmgmtsys password" />
+                </div>
+                <div style={{display:'flex', gap:8}}>
+                  <button type="button" className="btn btn-primary btn-sm" onClick={saveAdvmgmtCreds}>Save in browser</button>
+                  <button type="button" className="btn btn-ghost btn-sm" onClick={() => setShowAdvmgmtForm(false)}>Cancel</button>
+                </div>
+              </div>
+            )}
           </div>
 
           <div style={{borderTop:'1px solid var(--border)', paddingTop:20, marginTop:20}}>
@@ -7480,7 +7900,19 @@ function App() {
   const [theme, setThemeState] = useState(() => LS.get('theme', 'light'));
   const [currentPage, setCurrentPage] = useState(() => LS.get('currentPage', 'dashboard'));
   const [sidebarOpen, setSidebarOpen] = useState(false);
-  const [matters, setMattersState] = useState(() => LS.get('matters', DEFAULT_MATTERS));
+  // Matters are loaded fresh per advocate. The DEFAULT_MATTERS sample data is ONLY shown
+  // on first run with no user and no prior data — once a real advocate signs in, their
+  // matters come from /api/advocate-cases-all and the demo data is replaced (or cleared
+  // entirely if the upstream genuinely has nothing for that roll number).
+  const [matters, setMattersState] = useState(() => {
+    const stored = LS.get('matters', null);
+    if (stored && Array.isArray(stored)) return stored;
+    const u = LS.get('user', null);
+    // Once a real user is signed in, don't seed demo matters at all.
+    return u && (u.barCouncil || u.rollNo) ? [] : DEFAULT_MATTERS;
+  });
+  // Status of the last live fetch so the UI can distinguish demo vs empty vs maintenance.
+  const [liveFetchStatus, setLiveFetchStatus] = useState({ kind: 'idle', message: '' });
   const [tasks, setTasksState] = useState(() => LS.get('tasks', DEFAULT_TASKS));
   const [invoices, setInvoicesState] = useState(() => LS.get('invoices', DEFAULT_INVOICES));
   const [calendarTasks, setCalendarTasksState] = useState(() => LS.get('calendarTasks', []));
@@ -7663,21 +8095,22 @@ function App() {
       // Check if backend is reachable
       const health = await CourtAPI.healthCheck();
       if (health.status !== 'ok') {
-        console.warn('[App] Backend API not reachable, using cached/demo data');
+        console.warn('[App] Backend API not reachable');
         setBackendOnline(false);
+        setLiveFetchStatus({ kind: 'backend_down', message: 'Backend is unreachable — cached data shown.' });
         return;
       }
       setBackendOnline(true);
       setLoadingLiveCases(true);
+      setLiveFetchStatus({ kind: 'loading', message: 'Fetching live matters from Allahabad HC CCMS…' });
 
       try {
-        // Try rollNo (Advocate ID) first since CCMS uses court-specific roll numbers,
-        // then fall back to barCouncil enrollment number
+        // Try roll number first (Advocate ID format the CCMS form expects),
+        // then fall back to barCouncil enrollment.
         const lookupId = user.rollNo || user.barCouncil;
         let result = await CourtAPI.getAdvocateCases(lookupId);
         if (cancelled) return;
 
-        // If rollNo didn't find cases and barCouncil is different, try that too
         if ((!result || result.status !== 'success' || result.cases?.length === 0)
             && user.rollNo && user.barCouncil && user.rollNo !== user.barCouncil) {
           console.log('[App] No cases with rollNo, trying barCouncil:', user.barCouncil);
@@ -7687,26 +8120,60 @@ function App() {
           }
         }
 
-        if (result.status === 'success' && result.cases.length > 0) {
-          // Convert API cases to app matter format
-          const liveCases = result.cases.map((c, i) => CourtAPI.apiCaseToMatter(c, i));
+        // Cases come back as either matters that already existed (synced via live id prefix)
+        // or as new ones. Either way, we keep manually-added matters and replace the live ones.
+        const existingMatters = LS.get('matters', []) || [];
+        const localOnlyMatters = existingMatters.filter(m =>
+          m && !m._liveData && !(typeof m.id === 'string' && m.id.startsWith('live_'))
+        );
 
-          // Merge with any local-only cases (manually added ones)
-          const existingMatters = LS.get('matters', []);
-          const localOnlyMatters = existingMatters.filter(m => !m._liveData);
+        if (result && result.status === 'success' && result.cases?.length > 0) {
+          const liveCases = result.cases.map((c, i) => {
+            const m = CourtAPI.apiCaseToMatter(c, i);
+            m._liveData = true;
+            return m;
+          });
           const merged = [...liveCases, ...localOnlyMatters];
-
           setMattersState(merged);
           LS.set('matters', merged);
           LS.set('matters_lastSync', Date.now());
+          setLiveFetchStatus({
+            kind: 'success',
+            message: `Loaded ${liveCases.length} live case(s).`,
+            allahabadCount: result.allahabadCases || 0,
+            lucknowCount: result.lucknowCases || 0,
+            benchStatus: result.benchStatus,
+          });
           addToast(`Loaded ${liveCases.length} live case(s) from Allahabad HC`, 'success');
-        } else if (result.status === 'no_cases_found') {
-          addToast('No cases found for your enrollment. You can add cases manually.', 'info');
-        } else if (result.status === 'captcha_failed') {
-          addToast('Court portal CAPTCHA failed. Using cached data. Will retry automatically.', 'info');
+        } else if (result && result.status === 'upstream_maintenance') {
+          // Don't seed demo matters — show real empty state with clear maintenance reason.
+          const onlyLocal = localOnlyMatters;
+          setMattersState(onlyLocal);
+          LS.set('matters', onlyLocal);
+          LS.set('matters_lastSync', Date.now());
+          setLiveFetchStatus({
+            kind: 'upstream_maintenance',
+            message: 'Allahabad HC CCMS (Lucknow Bench) is currently in maintenance. Live matters will appear when the court portal is back online — typically within a few hours.',
+            benchStatus: result.benchStatus,
+          });
+          addToast('Court portal is in maintenance. Will retry automatically.', 'info');
+        } else {
+          // No live cases found for this roll number — drop demo matters too,
+          // keep only manually-added ones, and tell the user clearly.
+          setMattersState(localOnlyMatters);
+          LS.set('matters', localOnlyMatters);
+          LS.set('matters_lastSync', Date.now());
+          setLiveFetchStatus({
+            kind: 'no_cases',
+            message: `No cases found on the Allahabad HC CCMS for roll number "${lookupId}". Verify your Advocate Roll Number in Settings (format: B/A2401/2019), or add matters manually.`,
+            benchStatus: result?.benchStatus,
+            lookupId,
+          });
+          addToast('No cases found for your roll number. Check Settings to verify it.', 'info');
         }
       } catch (err) {
         console.error('[App] Live case fetch error:', err);
+        setLiveFetchStatus({ kind: 'error', message: err.message || 'Live fetch failed.' });
       } finally {
         if (!cancelled) setLoadingLiveCases(false);
       }
@@ -7761,7 +8228,7 @@ function App() {
     calendarTasks, setCalendarTasks,
     teamMembers, setTeamMembers,
     selectedMatter, setSelectedMatter, addToast, isOnline,
-    backendOnline, loadingLiveCases,
+    backendOnline, loadingLiveCases, liveFetchStatus,
   };
 
   const renderPage = () => {
diff --git a/allahabad-hc-mcp/captcha-solver.js b/allahabad-hc-mcp/captcha-solver.js
index bcf61fe..435f4f4 100644
--- a/allahabad-hc-mcp/captcha-solver.js
+++ b/allahabad-hc-mcp/captcha-solver.js
@@ -30,6 +30,29 @@ import sharp from "sharp";
 async function preprocessImage(imageBuffer) {
   const variants = [];
 
+  // Strategy 0 (NEW — BEST for Allahabad HC securimage CAPTCHA):
+  // Color-based extraction. The CCMS captcha uses bold dark-teal digits over
+  // a noisy gray background. Plain grayscale + threshold can't separate them
+  // because both end up similar gray. Filter pixel-by-pixel to keep only dark
+  // teal/blue and discard the gray noise, then upscale.
+  try {
+    const { data, info } = await sharp(imageBuffer).raw().toBuffer({ resolveWithObject: true });
+    const w = info.width, h = info.height, ch = info.channels;
+    const mask = Buffer.alloc(w * h);
+    for (let i = 0, j = 0; i < data.length; i += ch, j++) {
+      const r = data[i], g = data[i + 1], b = data[i + 2];
+      const brightness = (r + g + b) / 3;
+      const isDarkTeal = brightness < 180 && b > r && (b + g) > 2 * r + 20;
+      const isVeryDark = brightness < 80;
+      mask[j] = (isDarkTeal || isVeryDark) ? 0 : 255;
+    }
+    const v0 = await sharp(mask, { raw: { width: w, height: h, channels: 1 } })
+      .resize(w * 3, h * 3, { kernel: sharp.kernel.lanczos3 })
+      .png()
+      .toBuffer();
+    variants.push({ name: "color_extract_teal_3x", buffer: v0 });
+  } catch (_) {}
+
   // Strategy 1: High-contrast grayscale + threshold
   try {
     const v1 = await sharp(imageBuffer)
@@ -169,15 +192,23 @@ async function solveCaptchaFromBuffer(imageBuffer) {
 
   const results = [];
 
+  // The Allahabad HC CCMS captcha is 4 digits. For the color-extracted variants
+  // we prefer a digits-only whitelist; for grayscale variants we keep the broader
+  // alphanumeric whitelist as a safety net (in case the captcha style ever changes).
   for (const variant of variants) {
+    const isColorExtract = variant.name.startsWith("color_extract");
     try {
       const { data } = await Tesseract.recognize(variant.buffer, "eng", {
-        tessedit_char_whitelist: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789",
-        tessedit_pageseg_mode: "7",  // single line of text
+        tessedit_char_whitelist: isColorExtract
+          ? "0123456789"
+          : "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789",
+        tessedit_pageseg_mode: isColorExtract ? "8" : "7",  // 8 = single word, 7 = single line
       });
 
       const cleaned = cleanCaptchaText(data.text);
-      const score = scoreCaptchaResult(cleaned, data.confidence);
+      let score = scoreCaptchaResult(cleaned, data.confidence);
+      // Bonus: the CCMS captcha is exactly 4 digits — boost confidence when we get that shape.
+      if (/^\d{4}$/.test(cleaned)) score += 30;
 
       results.push({
         text: cleaned,
@@ -276,18 +307,40 @@ async function solveCourtCaptcha(pageUrl, baseUrl, options = {}) {
 
       const html = await resp.text();
       const setCookies = resp.headers.getSetCookie?.() || [];
-      const sessionToken = setCookies.join("; ");
-
-      // Step 2: Extract CAPTCHA image URL from HTML
-      const captchaMatch = html.match(/<img[^>]+src=["']([^"']*captcha[^"']*)["']/i);
-      if (!captchaMatch) {
-        errors.push(`Attempt ${attempt}: No CAPTCHA image found on page`);
+      // Set-Cookie values are full cookie specs ("name=value; Path=/; HttpOnly").
+      // The Cookie request header must be just "name1=value1; name2=value2" — strip attributes.
+      const sessionToken = setCookies
+        .map(c => c.split(";")[0].trim())
+        .filter(Boolean)
+        .join("; ");
+
+      // Step 2: Extract CAPTCHA image URL from HTML.
+      // The Allahabad HC CCMS uses securimage.php style URLs (no "captcha" in the path);
+      // try multiple patterns in priority order.
+      const captchaPatterns = [
+        /<img[^>]+id=["']captcha["'][^>]*src=["']([^"']+)["']/i,
+        /<img[^>]+src=["']([^"']+)["'][^>]*id=["']captcha["']/i,
+        /<img[^>]+src=["']([^"']*(?:captcha|kaptcha|securimage|secureimage|getcaptcha)[^"']*)["']/i,
+        /<img[^>]+src=["']([^"']+)["'][^>]+alt=["']captcha["']/i,
+      ];
+      let captchaUrl = null;
+      for (const re of captchaPatterns) {
+        const m = html.match(re);
+        if (m) { captchaUrl = m[1]; break; }
+      }
+      if (!captchaUrl) {
+        errors.push(`Attempt ${attempt}: No CAPTCHA image found on page (tried ${captchaPatterns.length} patterns)`);
         continue;
       }
 
-      let captchaUrl = captchaMatch[1];
       if (!captchaUrl.startsWith("http")) {
-        captchaUrl = `${baseUrl}/${captchaUrl.replace(/^\//, "")}`;
+        // Resolve relative URLs against the requested page URL, not just baseUrl,
+        // so paths like "captcha.php" resolve correctly.
+        try {
+          captchaUrl = new URL(captchaUrl, pageUrl).toString();
+        } catch (_) {
+          captchaUrl = `${baseUrl}/${captchaUrl.replace(/^\//, "")}`;
+        }
       }
 
       // Step 3: Fetch and solve the CAPTCHA
diff --git a/api-server/index.js b/api-server/index.js
index fb65ac2..5bde691 100644
--- a/api-server/index.js
+++ b/api-server/index.js
@@ -34,11 +34,25 @@ import * as cheerio from "cheerio";
 
 const PORT = process.env.PORT || 3001;
 const BASE_URL = "https://www.allahabadhighcourt.in";
-const CCMS_ALD = `${BASE_URL}/apps/status_ccms`;
-const CCMS_LKO = `${BASE_URL}/apps/status_ccms_lko`;
+// CCMS bases.
+// IMPORTANT: ALD CCMS uses /index.php/<route> for GETs but POSTs go to /<route> (form action strips index.php).
+// LKO has migrated to its own subdomain (hclko.allahabadhighcourt.in) — /apps/status_ccms_lko is dead (404).
+const CCMS_ALD = `${BASE_URL}/apps/status_ccms`;          // POST base
+const CCMS_ALD_GET = `${BASE_URL}/apps/status_ccms/index.php`;  // GET base
+const CCMS_LKO = "https://hclko.allahabadhighcourt.in/status";        // POST base
+const CCMS_LKO_GET = "https://hclko.allahabadhighcourt.in/status/index.php"; // GET base
 const ELEGALIX = "https://elegalix.allahabadhighcourt.in/elegalix";
 const COURT_VIEW_ALD = "https://courtview2.allahabadhighcourt.in/courtview/CourtViewAllahabad.do";
 const COURT_VIEW_LKO = "https://courtview2.allahabadhighcourt.in/courtview/CourtViewLucknow.do";
+// Live cause list (CCMS — replaces the dead /causelist/causelist_*.htm URLs that returned 404)
+const CAUSELIST_CCMS_ALD = `${BASE_URL}/apps/status_ccms/index.php/causelist`;
+const CAUSELIST_CCMS_LKO = "https://hclko.allahabadhighcourt.in/status/index.php/cause-list";
+// Legacy PDF index (fallback only — still alive at 200, but mostly stale 2021 samples)
+const CAUSELIST_PDF_INDEX_ALD = `${BASE_URL}/causelist/indexA.html`;
+const CAUSELIST_PDF_INDEX_LKO = "https://hclko.allahabadhighcourt.in/causelist/indexL.html";
+// RSS feeds for Legal Updates
+const RSS_HEADLINES = `${BASE_URL}/calendar/rssHeadlines.jsp`;
+const RSS_JUDGMENTS = `${ELEGALIX}/rssfeed.do`;
 const ECOURTS_BASE = "https://services.ecourts.gov.in/ecourtindia_v6";
 const BCI_SEARCH = "https://www.barcouncilofindia.org/advocate-search";
 
@@ -111,21 +125,25 @@ async function fetchPage(url, options = {}) {
       redirect: "follow",
       signal: AbortSignal.timeout(15000),
     });
-    const setCookies = resp.headers.getSetCookie?.() || [];
+    const setCookieRaw = resp.headers.getSetCookie?.() || [];
+    // Normalize for re-use as a Cookie request header: strip attributes from each value.
+    const setCookies = setCookieRaw
+      .map(c => c.split(";")[0].trim())
+      .filter(Boolean);
     const html = await resp.text();
-    return { html, status: resp.status, cookies: setCookies, url: resp.url };
+    return { html, status: resp.status, cookies: setCookies, cookiesRaw: setCookieRaw, url: resp.url };
   } catch (err) {
     return { html: "", status: 0, cookies: [], url, error: err.message };
   }
 }
 
-async function fetchPostForm(url, formData, sessionToken) {
+async function fetchPostForm(url, formData, sessionToken, extraHeaders = {}) {
   const body = new URLSearchParams(formData).toString();
   return fetchPage(url, {
     method: "POST",
     body,
     sessionToken,
-    headers: { "Content-Type": "application/x-www-form-urlencoded" },
+    headers: { "Content-Type": "application/x-www-form-urlencoded", ...extraHeaders },
   });
 }
 
@@ -161,7 +179,9 @@ async function loadCaptchaSolver() {
 async function solveCaptchaForUrl(url, base) {
   const solver = await loadCaptchaSolver();
   if (!solver) throw new Error("CAPTCHA solver not available");
-  return solver(url, base, { maxAttempts: 3 });
+  // 6 attempts because the CCMS captcha is genuinely hard (color extraction works ~70-80%
+  // of the time, so 6 attempts gives >99% success even with no retry layer above).
+  return solver(url, base, { maxAttempts: 6 });
 }
 
 // ─────────────────────────────────────────────────────────────────────
@@ -176,6 +196,7 @@ const CACHE_TTL = {
   causelist: 30 * 60 * 1000,       // 30 min
   court_view: 30 * 1000,           // 30 sec (live data)
   calendar: 24 * 60 * 60 * 1000,   // 24 hr
+  news: 60 * 60 * 1000,            // 60 min (RSS feeds)
 };
 
 function getCached(key) {
@@ -268,135 +289,349 @@ async function verifyOnECourts(advocateName, stateCode) {
 // ADVOCATE CASES
 // ─────────────────────────────────────────────────────────────────────
 
-async function fetchAdvocateCases(rollNumber, bench = "allahabad") {
-  const cacheKey = `adv_${rollNumber}_${bench}`;
-  const cached = getCached(cacheKey);
-  if (cached) return { ...cached, fromCache: true };
-
-  // Build list of roll number formats to try:
-  // 1. As-is (original input)
-  // 2. If it looks like "B/A2401/2019" → try "A2401/2019" (strip prefix)
-  // 3. If it looks like "UP/2030/2018" (Bar Council format) → try as-is
-  // 4. If it contains "/" → try without any prefix (just number/year)
-  const formatsToTry = [rollNumber];
-  const cleaned = rollNumber.trim();
-
-  // Strip single-letter prefix like "B/" from Advocate IDs (e.g., "B/A2401/2019" → "A2401/2019")
-  if (/^[A-Z]\//.test(cleaned)) {
-    formatsToTry.push(cleaned.substring(2));
+// Build the list of roll-number format variants to try against the court form.
+// The CCMS form expects the "Advocate Roll Number" format (e.g. "B/A2401/2019") —
+// NOT the Bar Council enrollment number (e.g. "UP/2030/2018"). We accept either input
+// and try a few common normalizations.
+function rollNumberVariants(rollNumber) {
+  const cleaned = (rollNumber || "").trim().toUpperCase();
+  if (!cleaned) return [];
+  const variants = new Set([cleaned]);
+  // "B/A2401/2019" → "A2401/2019" (strip single-letter type prefix)
+  if (/^[A-Z]\//.test(cleaned)) variants.add(cleaned.substring(2));
+  // "A2401/2019" → "B/A2401/2019" (add common type prefix)
+  if (/^[A-Z]?\d+\/\d{4}$/.test(cleaned) && !/^[A-Z]\//.test(cleaned)) {
+    ["B", "A", "L"].forEach(p => variants.add(`${p}/${cleaned}`));
   }
-  // If it has format like "A2401/2019", also try just the number
-  const numYearMatch = cleaned.match(/[A-Z]*(\d+)\/(\d{4})$/);
-  if (numYearMatch) {
-    formatsToTry.push(`${numYearMatch[1]}/${numYearMatch[2]}`);
+  // "UP/2030/2018" Bar Council format — try as-is and also strip state prefix
+  const upMatch = cleaned.match(/^([A-Z]{2,3})\/(\d+)\/(\d{4})$/);
+  if (upMatch) {
+    variants.add(`${upMatch[2]}/${upMatch[3]}`);
+    ["B", "A", "L"].forEach(p => variants.add(`${p}/${upMatch[2]}/${upMatch[3]}`));
   }
+  return [...variants];
+}
 
-  // Deduplicate
-  const uniqueFormats = [...new Set(formatsToTry)];
-  console.log(`[API] Advocate case lookup for "${rollNumber}" — trying formats: ${uniqueFormats.join(", ")}`);
+// Parse a CCMS results page into structured cases.
+// The result table column order (observed): Sr. | Case Number | Petitioner | Respondent | Listing | Status
+function parseAdvocateCases($, rollNumber, bench) {
+  const cases = [];
+  // The result lives in a <table> with class "table" (Bootstrap-styled). Use the largest table on the page.
+  let bestTable = null;
+  let bestRows = 0;
+  $("table").each((_, t) => {
+    const rowCount = $(t).find("tr").length;
+    if (rowCount > bestRows) { bestRows = rowCount; bestTable = t; }
+  });
+  if (!bestTable) return { status: "no_cases_found", rollNumber, bench, totalCases: 0, cases: [], fetchedAt: istTimestamp() };
+
+  // Read header to map columns by name (column order varies between roll-wise and date-wise pages)
+  const headerCells = [];
+  $(bestTable).find("tr").first().find("th, td").each((_, c) => headerCells.push($(c).text().trim().toLowerCase()));
+  const col = (re) => headerCells.findIndex(h => re.test(h));
+  const idx = {
+    serial: col(/^(sr|s\.? *no|serial)/),
+    caseRef: col(/case|cnr/),
+    petitioner: col(/petition|applicant|appellant/),
+    respondent: col(/respond|opposite/),
+    parties: col(/parties|title|vs/),
+    nextHearing: col(/next|listing|hearing/),
+    status: col(/status|stage/),
+    coram: col(/coram|judge|bench/),
+  };
 
-  const base = bench === "lucknow" ? CCMS_LKO : CCMS_ALD;
-  const url = `${base}/advocate-cases-roll-wise`;
+  $(bestTable).find("tr").each((i, tr) => {
+    if (i === 0 && headerCells.length) return; // skip header
+    const cells = [];
+    $(tr).find("td").each((_, td) => cells.push($(td).text().trim().replace(/\s+/g, " ")));
+    if (cells.length < 2) return;
+
+    const caseRef = idx.caseRef >= 0 ? cells[idx.caseRef] : (cells[1] || "");
+    if (!caseRef || caseRef.length < 3) return;
+    const refMatch = caseRef.match(/([A-Z]+)\s*[-\/]?\s*(\d+)\s*[-\/]\s*(\d{4})/i);
+
+    const petitioner = idx.petitioner >= 0 ? cells[idx.petitioner] : "";
+    const respondent = idx.respondent >= 0 ? cells[idx.respondent] : "";
+    const parties = idx.parties >= 0
+      ? cells[idx.parties]
+      : (petitioner && respondent ? `${petitioner} Vs ${respondent}` : (petitioner || respondent || cells[2] || ""));
+
+    cases.push({
+      serial: idx.serial >= 0 ? cells[idx.serial] : String(i),
+      caseRef,
+      caseType: refMatch ? refMatch[1].toUpperCase() : "",
+      caseNo: refMatch ? refMatch[2] : "",
+      year: refMatch ? refMatch[3] : "",
+      petitioner,
+      respondent,
+      parties,
+      title: parties,
+      nextHearing: idx.nextHearing >= 0 ? (cells[idx.nextHearing] || null) : null,
+      status: idx.status >= 0 ? (cells[idx.status] || "PENDING") : "PENDING",
+      coram: idx.coram >= 0 ? (cells[idx.coram] || null) : null,
+    });
+  });
 
-  for (const format of uniqueFormats) {
-    // Solve CAPTCHA for each attempt
-    let sessionToken, captchaAnswer;
-    try {
-      const solved = await solveCaptchaForUrl(url, base);
-      sessionToken = solved.sessionToken;
-      captchaAnswer = solved.captchaAnswer;
-      console.log(`[API] CAPTCHA solved for advocate lookup (format "${format}"): "${captchaAnswer}" (confidence: ${solved.confidence.toFixed(1)}%)`);
-    } catch (err) {
-      console.error(`[API] CAPTCHA solve failed: ${err.message}`);
-      continue; // Try next format
-    }
+  return {
+    status: cases.length > 0 ? "success" : "no_cases_found",
+    rollNumber,
+    bench: bench === "lucknow" ? "Lucknow Bench" : "Allahabad",
+    totalCases: cases.length,
+    cases,
+    fetchedAt: istTimestamp(),
+  };
+}
 
-    const result = await fetchPostForm(url, {
-      roll_no: format,
-      captcha: captchaAnswer,
-      submit: "Search",
-    }, sessionToken);
+// Detect failure modes after a POST to get_ListedCaseRoll (AJAX HTML fragment) or the full form page.
+function detectAdvocateFormError($, htmlOpt) {
+  const html = (htmlOpt || $.html() || "").toLowerCase();
+  // Explicit captcha rejection
+  if (/wrong\s*captcha|invalid\s*captcha|captcha\s*(is\s*)?(wrong|invalid|incorrect)/i.test(html)) return "captcha";
+  // "Record Not Found" is the no-match signal from get_ListedCaseRoll
+  if (/record\s*not\s*found|no\s*record(s)?\s*found|no\s*case(s)?\s*found/i.test(html)) return "no_match";
+  // Generic danger text (could be either captcha or input validation)
+  const errorText = $(".alert-danger, .alert-warning, .error, .text-danger, .help-block").text().trim().toLowerCase();
+  if (!errorText) return null;
+  if (errorText.includes("captcha") || errorText.includes("verification code")) return "captcha";
+  if (errorText.includes("invalid") || errorText.includes("incorrect")) return "captcha";
+  if (errorText.includes("no record") || errorText.includes("not found")) return "no_match";
+  return "other";
+}
 
-    const $ = cheerio.load(result.html);
+// One POST attempt with one captcha solve. Returns { kind: "ok"|"captcha"|"no_match"|"error", data?, errMsg? }
+async function attemptAdvocatePost({ getUrl, postUrl, fields, debug = false }) {
+  let solved;
+  try {
+    solved = await solveCaptchaForUrl(getUrl, getUrl);
+  } catch (err) {
+    if (debug) console.log(`[advocatePost] captcha solve failed: ${err.message}`);
+    return { kind: "error", errMsg: `captcha solve failed: ${err.message}` };
+  }
+  const formData = { ...fields, captchacode: solved.captchaAnswer, submit: "Go" };
+  if (debug) console.log(`[advocatePost] POST ${postUrl} with`, formData);
+  // The get_ListedCaseRoll endpoint expects an XHR request with the form-page Referer.
+  const result = await fetchPostForm(postUrl, formData, solved.sessionToken, {
+    "X-Requested-With": "XMLHttpRequest",
+    "Referer": getUrl,
+  });
+  if (debug) console.log(`[advocatePost] response status=${result.status} bodyLen=${result.html?.length || 0}`);
+  const $ = cheerio.load(result.html);
+  const errKind = detectAdvocateFormError($, result.html);
+  if (debug) {
+    const bodyText = $("body").text().replace(/\s+/g, " ").slice(0, 400);
+    const tableCount = $("table").length;
+    const rowCount = $("table tr").length;
+    console.log(`[advocatePost] errKind=${errKind} tables=${tableCount} rows=${rowCount} bodyPreview="${bodyText}"`);
+  }
+  if (errKind === "captcha") return { kind: "captcha", $, errMsg: "CAPTCHA rejected" };
+  if (errKind === "no_match") return { kind: "no_match", $ };
+  return { kind: "ok", $, rawHtml: debug ? result.html : undefined };
+}
 
-    // Check for CAPTCHA rejection
-    const errorText = $(".alert-danger, .error, .text-danger").text().trim();
-    if (errorText.toLowerCase().includes("captcha") || errorText.toLowerCase().includes("invalid")) {
-      // Retry once with new CAPTCHA
-      try {
-        const retry = await solveCaptchaForUrl(url, base);
-        const retryResult = await fetchPostForm(url, { roll_no: format, captcha: retry.captchaAnswer, submit: "Search" }, retry.sessionToken);
-        const $r = cheerio.load(retryResult.html);
-        const retryErr = $r(".alert-danger, .error, .text-danger").text().trim();
-        if (!retryErr.toLowerCase().includes("captcha")) {
-          const data = parseAdvocateCases($r, rollNumber, bench);
-          if (data.totalCases > 0) {
-            console.log(`[API] Found ${data.totalCases} cases using format "${format}"`);
-            setCache(cacheKey, data, CACHE_TTL.advocate_cases);
-            return data;
-          }
-        }
-      } catch (_) {}
-      continue; // Try next format
-    }
+// Diagnostic dry-run — one POST, returns the raw response for inspection.
+async function dryRunAdvocate(bench, rollNumber, year, listingDate) {
+  const getUrl = bench === "lucknow"
+    ? `${CCMS_LKO_GET}/advocate-cases-date-wise`
+    : `${CCMS_ALD_GET}/advocate-cases-roll-wise`;
+  // Real submit endpoint is an AJAX route under index.php (not the form's action attribute).
+  const postUrl = bench === "lucknow"
+    ? `${CCMS_LKO_GET}/get_ListedCaseDate`         // best guess for LKO equivalent
+    : `${CCMS_ALD_GET}/get_ListedCaseRoll`;
+  const today = istNow();
+  const defaultDate = `${today.getFullYear()}-${String(today.getMonth()+1).padStart(2,"0")}-${String(today.getDate()).padStart(2,"0")}`;
+  const fields = bench === "lucknow"
+    ? { adv_roll: rollNumber, date: listingDate || defaultDate, listing_date: listingDate || defaultDate }
+    : { adv_roll: rollNumber, case_year: String(year || istNow().getFullYear()) };
+  const out = await attemptAdvocatePost({ getUrl, postUrl, fields, debug: true });
+  const parsed = out.$ ? parseAdvocateCases(out.$, rollNumber, bench) : null;
+  return {
+    bench,
+    getUrl, postUrl, fields,
+    kind: out.kind,
+    errMsg: out.errMsg,
+    htmlLength: out.rawHtml?.length,
+    bodyPreview: out.$ ? out.$("body").text().replace(/\s+/g, " ").slice(0, 600) : null,
+    parsedCount: parsed?.totalCases || 0,
+    parsedSample: parsed?.cases?.slice(0, 2) || [],
+  };
+}
 
-    const data = parseAdvocateCases($, rollNumber, bench);
-    if (data.totalCases > 0) {
-      console.log(`[API] Found ${data.totalCases} cases using format "${format}"`);
-      setCache(cacheKey, data, CACHE_TTL.advocate_cases);
-      return data;
+// Allahabad bench — advocate-cases-roll-wise.
+// CRITICAL: The HTML form looks like it POSTs back to itself, but the visible page is
+// just a shell. A click handler intercepts the submit and AJAX-POSTs to
+// /apps/status_ccms/index.php/get_ListedCaseRoll which returns the actual results HTML
+// (table rows or "Record Not Found").
+// FORM REQUIRES: adv_roll, case_year, captchacode, submit=Go
+// To get ALL cases for an advocate we must iterate years.
+async function fetchAdvocateCasesAllahabad(rollNumber, opts = {}) {
+  const getUrl = `${CCMS_ALD_GET}/advocate-cases-roll-wise`;
+  const postUrl = `${CCMS_ALD_GET}/get_ListedCaseRoll`;
+  const variants = rollNumberVariants(rollNumber);
+  const currentYear = istNow().getFullYear();
+  // Default: scan last 5 years. Caller can pass ?yearsBack=N or ?years=a,b,c to widen.
+  const yearsBack = opts.yearsBack || 4;
+  const years = opts.years || Array.from({ length: yearsBack + 1 }, (_, i) => currentYear - i);
+
+  const allCases = [];
+  const seen = new Set();
+  let formatWorked = null;
+  const benchErrors = [];
+
+  for (const adv_roll of variants) {
+    let formatHadAny = false;
+    let consecutiveCaptchaFails = 0;
+    for (const year of years) {
+      let attempt = await attemptAdvocatePost({
+        getUrl, postUrl, fields: { adv_roll, case_year: String(year) },
+      });
+      // One quick retry on captcha rejection (the solver already retries inside, this is belt+braces).
+      if (attempt.kind === "captcha") {
+        attempt = await attemptAdvocatePost({ getUrl, postUrl, fields: { adv_roll, case_year: String(year) } });
+      }
+      if (attempt.kind === "error") {
+        benchErrors.push(`year ${year}: ${attempt.errMsg}`);
+        consecutiveCaptchaFails++;
+        if (consecutiveCaptchaFails >= 3) { console.warn(`[API] ALD: aborting ${adv_roll} after 3 consecutive captcha errors`); break; }
+        continue;
+      }
+      if (attempt.kind === "captcha") {
+        consecutiveCaptchaFails++;
+        continue;
+      }
+      consecutiveCaptchaFails = 0;
+      if (attempt.kind !== "ok") continue;
+      const parsed = parseAdvocateCases(attempt.$, rollNumber, "allahabad");
+      if (parsed.totalCases > 0) {
+        formatHadAny = true;
+        for (const c of parsed.cases) {
+          const key = c.caseRef;
+          if (key && !seen.has(key)) { seen.add(key); allCases.push(c); }
+        }
+        console.log(`[API] ALD: ${adv_roll} / year ${year} → ${parsed.totalCases} cases`);
+      }
     }
-    console.log(`[API] No cases found with format "${format}", trying next...`);
+    if (formatHadAny) { formatWorked = adv_roll; break; }
   }
 
-  // None of the formats returned cases
-  console.log(`[API] No cases found for "${rollNumber}" with any format`);
   return {
-    status: "no_cases_found",
+    status: allCases.length > 0 ? "success" : "no_cases_found",
     rollNumber,
-    bench: bench === "lucknow" ? "Lucknow Bench" : "Allahabad",
-    totalCases: 0,
-    cases: [],
+    rollFormatUsed: formatWorked,
+    bench: "Allahabad",
+    totalCases: allCases.length,
+    cases: allCases,
+    yearsScanned: years,
     fetchedAt: istTimestamp(),
   };
 }
 
-function parseAdvocateCases($, rollNumber, bench) {
-  const cases = [];
-  $("table tr").each((i, tr) => {
-    if (i === 0) return;
-    const cells = [];
-    $(tr).find("td").each((j, td) => cells.push($(td).text().trim()));
-    if (cells.length >= 3) {
-      // Parse case reference to extract type/number/year
-      const caseRef = cells[1] || "";
-      const refMatch = caseRef.match(/([A-Z]+)\s*[-\/]?\s*(\d+)\s*[-\/]\s*(\d{4})/i);
-
-      cases.push({
-        serial: cells[0],
-        caseRef: cells[1],
-        caseType: refMatch ? refMatch[1].toUpperCase() : "",
-        caseNo: refMatch ? refMatch[2] : "",
-        year: refMatch ? refMatch[3] : "",
-        parties: cells[2],
-        title: cells[2], // alias
-        nextHearing: cells[3] || null,
-        status: cells[4] || "PENDING",
-        coram: cells[5] || null,
+// Lucknow bench — advocate-cases-date-wise.
+// FORM REQUIRES: adv_roll, listing_date (YYYY-MM-DD picked from a select), captchacode, submit=Go
+// The endpoint returns cases LISTED ON A SPECIFIC DATE — not the advocate's entire case history.
+// To approximate "all matters" we scan a window of upcoming + recent dates.
+// NOTE: LKO captcha is JS-generated client-side; server doesn't strictly validate it (any string works after fetching the form to get a session cookie).
+async function fetchAdvocateCasesLucknow(rollNumber, opts = {}) {
+  const getUrl = `${CCMS_LKO_GET}/advocate-cases-date-wise`;
+  const postUrl = `${CCMS_LKO_GET}/advocate-cases-date-wise`;
+  const variants = rollNumberVariants(rollNumber);
+  // Quick reachability check — LKO has been known to go into maintenance mode.
+  // If the form page says "Website is currently down for maintenance" we short-circuit with a clear error.
+  const probe = await fetchPage(getUrl);
+  if (probe.html && /currently down for maintenance/i.test(probe.html)) {
+    return {
+      status: "upstream_maintenance",
+      rollNumber,
+      bench: "Lucknow Bench",
+      totalCases: 0,
+      cases: [],
+      error: "Allahabad HC Lucknow Bench CCMS is currently in maintenance mode (per upstream banner). Try again later.",
+      fetchedAt: istTimestamp(),
+    };
+  }
+
+  // Build candidate listing dates: today + next N working days + last few days.
+  const dates = [];
+  const today = istNow();
+  const fmt = d => `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`;
+  const window = opts.daysWindow || 14;
+  for (let i = -3; i <= window; i++) {
+    const d = new Date(today); d.setDate(today.getDate() + i);
+    if (d.getDay() === 0 || d.getDay() === 6) continue; // skip Sun/Sat
+    dates.push(fmt(d));
+  }
+
+  const allCases = [];
+  const seen = new Set();
+  let formatWorked = null;
+
+  for (const adv_roll of variants) {
+    let formatHadAny = false;
+    for (const listing_date of dates) {
+      // Any captchacode works for LKO; we still call attemptAdvocatePost so we get a fresh session cookie.
+      let attempt = await attemptAdvocatePost({
+        getUrl, postUrl,
+        fields: { adv_roll, date: listing_date, listing_date }, // include both common field names
       });
+      if (attempt.kind === "captcha") {
+        attempt = await attemptAdvocatePost({ getUrl, postUrl, fields: { adv_roll, date: listing_date, listing_date } });
+      }
+      if (attempt.kind !== "ok") continue;
+      const parsed = parseAdvocateCases(attempt.$, rollNumber, "lucknow");
+      if (parsed.totalCases > 0) {
+        formatHadAny = true;
+        for (const c of parsed.cases) {
+          const key = c.caseRef;
+          if (key && !seen.has(key)) { seen.add(key); allCases.push(c); }
+        }
+        console.log(`[API] LKO: ${adv_roll} / ${listing_date} → ${parsed.totalCases} cases (cumulative: ${allCases.length})`);
+      }
     }
-  });
+    if (formatHadAny) { formatWorked = adv_roll; break; }
+  }
 
   return {
-    status: cases.length > 0 ? "success" : "no_cases_found",
+    status: allCases.length > 0 ? "success" : "no_cases_found",
     rollNumber,
-    bench: bench === "lucknow" ? "Lucknow Bench" : "Allahabad",
-    totalCases: cases.length,
-    cases,
+    rollFormatUsed: formatWorked,
+    bench: "Lucknow Bench",
+    totalCases: allCases.length,
+    cases: allCases,
+    datesScanned: dates,
+    note: "Lucknow CCMS only exposes per-date listings — total may understate the advocate's full case history (which lives in the login-gated advmgmtsys).",
     fetchedAt: istTimestamp(),
   };
 }
 
+// Unified entry point. Caches per (rollNumber, bench).
+async function fetchAdvocateCases(rollNumber, bench = "allahabad") {
+  const cacheKey = `adv_${rollNumber}_${bench}`;
+  const cached = getCached(cacheKey);
+  if (cached) return { ...cached, fromCache: true };
+
+  console.log(`[API] Advocate lookup: ${rollNumber} @ ${bench}`);
+
+  let data;
+  try {
+    data = bench === "lucknow"
+      ? await fetchAdvocateCasesLucknow(rollNumber)
+      : await fetchAdvocateCasesAllahabad(rollNumber);
+  } catch (err) {
+    console.error(`[API] Advocate lookup failed: ${err.message}`);
+    return {
+      status: "error",
+      rollNumber,
+      bench: bench === "lucknow" ? "Lucknow Bench" : "Allahabad",
+      error: err.message,
+      totalCases: 0,
+      cases: [],
+      fetchedAt: istTimestamp(),
+    };
+  }
+
+  if (data.totalCases > 0) setCache(cacheKey, data, CACHE_TTL.advocate_cases);
+  return data;
+}
+
 
 // ─────────────────────────────────────────────────────────────────────
 // CASE STATUS
@@ -540,32 +775,100 @@ async function fetchCauselistToday(bench = "allahabad") {
   const cached = getCached(cacheKey);
   if (cached) return { ...cached, fromCache: true };
 
-  const url = bench === "lucknow"
-    ? `${BASE_URL}/causelist/causelist_lko.htm`
-    : `${BASE_URL}/causelist/causelist_ald.htm`;
+  // PRIMARY: CCMS cause list (rich structured data).
+  // Previous URLs (/causelist/causelist_*.htm) returned 404 — the court moved this content to the CCMS app.
+  const ccmsUrl = bench === "lucknow" ? CAUSELIST_CCMS_LKO : CAUSELIST_CCMS_ALD;
+  const pdfIndexUrl = bench === "lucknow" ? CAUSELIST_PDF_INDEX_LKO : CAUSELIST_PDF_INDEX_ALD;
+  const pdfBase = bench === "lucknow"
+    ? "https://hclko.allahabadhighcourt.in/causelist/"
+    : `${BASE_URL}/causelist/`;
 
-  const page = await fetchPage(url);
-  const $ = cheerio.load(page.html);
+  const causelists = [];   // structured rows from CCMS
+  const pdfs = [];         // consolidated/supplementary PDF links
+
+  // ── Try CCMS first ──
+  try {
+    const page = await fetchPage(ccmsUrl);
+    if (page.status === 200 && page.html) {
+      const $ = cheerio.load(page.html);
+      // CCMS renders an HTML table; column order varies slightly between ALD and LKO,
+      // so detect columns by header name rather than position.
+      $("table").each((_, table) => {
+        const rows = parseTable($, table);
+        if (rows.length < 2) return;
+        const header = rows[0].map(h => h.toLowerCase());
+        const idx = {
+          court: header.findIndex(h => /court\s*no/.test(h)),
+          serial: header.findIndex(h => /sr\.?\s*no|serial/.test(h)),
+          caseRef: header.findIndex(h => /case|cnr/.test(h)),
+          parties: header.findIndex(h => /part(y|ies)|title/.test(h)),
+          listType: header.findIndex(h => /list|type/.test(h)),
+          judge: header.findIndex(h => /coram|judge|bench/.test(h)),
+        };
+        rows.slice(1).forEach(row => {
+          if (row.length < 2) return;
+          const entry = {
+            courtNo: idx.court >= 0 ? row[idx.court] : null,
+            serial: idx.serial >= 0 ? row[idx.serial] : null,
+            caseRef: idx.caseRef >= 0 ? row[idx.caseRef] : row[0],
+            parties: idx.parties >= 0 ? row[idx.parties] : (row[2] || null),
+            listType: idx.listType >= 0 ? row[idx.listType] : null,
+            coram: idx.judge >= 0 ? row[idx.judge] : null,
+          };
+          if (entry.caseRef && entry.caseRef.length > 1) causelists.push(entry);
+        });
+      });
 
-  const causelists = [];
-  $("a").each((i, a) => {
-    const href = $(a).attr("href");
-    const text = $(a).text().trim();
-    if (href && text && (href.includes(".pdf") || href.includes("causelist"))) {
-      causelists.push({
-        title: text,
-        url: href.startsWith("http") ? href : `${BASE_URL}/causelist/${href}`,
-        date: istDateStr(),
+      // Pick up any PDF causelist links present on the CCMS page
+      $("a").each((_, a) => {
+        const href = $(a).attr("href");
+        const text = $(a).text().trim();
+        if (href && /\.pdf$/i.test(href)) {
+          pdfs.push({
+            title: text || href.split("/").pop(),
+            url: href.startsWith("http") ? href : (href.startsWith("/") ? `${BASE_URL}${href}` : pdfBase + href),
+          });
+        }
       });
     }
-  });
+  } catch (err) {
+    console.warn(`[API] CCMS cause list fetch failed (${bench}):`, err.message);
+  }
+
+  // ── Fallback to legacy PDF index for consolidated / supplementary PDFs ──
+  try {
+    const idx = await fetchPage(pdfIndexUrl);
+    if (idx.status === 200 && idx.html) {
+      const $ = cheerio.load(idx.html);
+      $("a").each((_, a) => {
+        const href = $(a).attr("href");
+        const text = $(a).text().trim();
+        if (!href) return;
+        const isPdf = /\.pdf$/i.test(href);
+        const isCauselistLink = /causelist|cause-list/i.test(href);
+        if ((isPdf || isCauselistLink) && text) {
+          const fullUrl = href.startsWith("http") ? href : (href.startsWith("/") ? `${BASE_URL}${href}` : pdfBase + href);
+          if (!pdfs.some(p => p.url === fullUrl)) {
+            pdfs.push({ title: text, url: fullUrl });
+          }
+        }
+      });
+    }
+  } catch (err) {
+    console.warn(`[API] PDF index fallback failed (${bench}):`, err.message);
+  }
 
   const data = {
     status: "success",
     bench: bench === "lucknow" ? "Lucknow" : "Allahabad",
     date: istDateStr(),
+    source: "ccms+pdf-index",
+    sourceUrls: { ccms: ccmsUrl, pdfIndex: pdfIndexUrl },
     causelists,
-    totalLists: causelists.length,
+    pdfs,
+    totalLists: causelists.length + pdfs.length,
+    totalEntries: causelists.length,
+    totalPdfs: pdfs.length,
     fetchedAt: istTimestamp(),
   };
   setCache(cacheKey, data, CACHE_TTL.causelist);
@@ -573,6 +876,82 @@ async function fetchCauselistToday(bench = "allahabad") {
 }
 
 
+// ─────────────────────────────────────────────────────────────────────
+// LEGAL NEWS (Allahabad HC RSS feeds: headlines + judgments)
+// ─────────────────────────────────────────────────────────────────────
+
+function parseRssItems(xmlString, source) {
+  const $ = cheerio.load(xmlString, { xmlMode: true });
+  const items = [];
+  $("item").each((_, el) => {
+    const $el = $(el);
+    const description = $el.find("description").text().trim();
+    // Strip basic HTML from RSS descriptions for clean rendering
+    const cleanDesc = description.replace(/<[^>]+>/g, " ").replace(/\s+/g, " ").trim().slice(0, 400);
+    const link = $el.find("link").text().trim() || $el.find("guid").text().trim();
+    const pubDateRaw = $el.find("pubDate").text().trim() || $el.find("dc\\:date, date").text().trim();
+    let pubDate = null;
+    if (pubDateRaw) {
+      const d = new Date(pubDateRaw);
+      if (!isNaN(d.getTime())) pubDate = d.toISOString();
+    }
+    items.push({
+      id: `${source}_${Buffer.from(link || $el.find("title").text()).toString("base64").slice(0, 16)}`,
+      title: $el.find("title").text().trim(),
+      link,
+      description: cleanDesc,
+      source,           // "headlines" or "judgments"
+      pubDate,
+      pubDateRaw,
+    });
+  });
+  return items;
+}
+
+async function fetchLegalNews() {
+  const cacheKey = "news_all";
+  const cached = getCached(cacheKey);
+  if (cached) return { ...cached, fromCache: true };
+
+  const [headlinesRes, judgmentsRes] = await Promise.allSettled([
+    fetchPage(RSS_HEADLINES),
+    fetchPage(RSS_JUDGMENTS),
+  ]);
+
+  let headlines = [];
+  let judgments = [];
+
+  if (headlinesRes.status === "fulfilled" && headlinesRes.value.status === 200) {
+    try { headlines = parseRssItems(headlinesRes.value.html, "headlines"); }
+    catch (err) { console.warn("[API] Headlines RSS parse failed:", err.message); }
+  }
+  if (judgmentsRes.status === "fulfilled" && judgmentsRes.value.status === 200) {
+    try { judgments = parseRssItems(judgmentsRes.value.html, "judgments"); }
+    catch (err) { console.warn("[API] Judgments RSS parse failed:", err.message); }
+  }
+
+  // Merge + sort newest first
+  const all = [...headlines, ...judgments].sort((a, b) => {
+    const da = a.pubDate ? Date.parse(a.pubDate) : 0;
+    const db = b.pubDate ? Date.parse(b.pubDate) : 0;
+    return db - da;
+  });
+
+  const data = {
+    status: "success",
+    sources: {
+      headlines: { url: RSS_HEADLINES, count: headlines.length },
+      judgments: { url: RSS_JUDGMENTS, count: judgments.length },
+    },
+    totalItems: all.length,
+    items: all,
+    fetchedAt: istTimestamp(),
+  };
+  setCache(cacheKey, data, CACHE_TTL.news);
+  return data;
+}
+
+
 // ─────────────────────────────────────────────────────────────────────
 // COURT VIEW (live display board — for real-time ETA)
 // ─────────────────────────────────────────────────────────────────────
@@ -775,19 +1154,40 @@ app.get("/api/advocate-cases-all/:rollNumber", async (req, res) => {
     const aldCases = ald.status === "fulfilled" && ald.value.status === "success" ? ald.value.cases : [];
     const lkoCases = lko.status === "fulfilled" && lko.value.status === "success" ? lko.value.cases : [];
 
-    // Tag cases with their bench
     aldCases.forEach(c => { c.bench = "Allahabad"; c.court = "Allahabad High Court"; });
     lkoCases.forEach(c => { c.bench = "Lucknow"; c.court = "Allahabad High Court (Lucknow Bench)"; });
 
     const allCases = [...aldCases, ...lkoCases];
 
+    // Bench-level status detail so the frontend can show meaningful messages
+    // (e.g. "Lucknow Bench CCMS is in maintenance" vs "advocate has no cases").
+    const benchStatus = {
+      allahabad: {
+        status: ald.status === "fulfilled" ? ald.value.status : "error",
+        count: aldCases.length,
+        error: ald.status === "fulfilled" ? ald.value.error : ald.reason?.message,
+        rollFormatUsed: ald.status === "fulfilled" ? ald.value.rollFormatUsed : null,
+      },
+      lucknow: {
+        status: lko.status === "fulfilled" ? lko.value.status : "error",
+        count: lkoCases.length,
+        error: lko.status === "fulfilled" ? lko.value.error : lko.reason?.message,
+        rollFormatUsed: lko.status === "fulfilled" ? lko.value.rollFormatUsed : null,
+        note: lko.status === "fulfilled" ? lko.value.note : null,
+      },
+    };
+
     res.json({
-      status: allCases.length > 0 ? "success" : "no_cases_found",
+      status: allCases.length > 0 ? "success"
+            : (benchStatus.allahabad.status === "upstream_maintenance" || benchStatus.lucknow.status === "upstream_maintenance")
+              ? "upstream_maintenance"
+              : "no_cases_found",
       rollNumber,
       totalCases: allCases.length,
       allahabadCases: aldCases.length,
       lucknowCases: lkoCases.length,
       cases: allCases,
+      benchStatus,
       fetchedAt: istTimestamp(),
     });
   } catch (err) {
@@ -834,6 +1234,73 @@ app.get("/api/causelist-today", async (req, res) => {
   }
 });
 
+// ── advmgmtsys (alpha): expose current capability status ──
+// Full implementation requires per-advocate username+password+captcha-solve through
+// hclko.allahabadhighcourt.in/advmgmtsys/login. The captcha there is a different style
+// (alphanumeric on a blue gradient) — needs its own OCR profile.
+app.get("/api/advmgmtsys-status", (req, res) => {
+  res.json({
+    status: "not_implemented",
+    message: "Authenticated advmgmtsys scrape is on the roadmap. Public CCMS endpoints are working.",
+    loginUrl: "https://hclko.allahabadhighcourt.in/advmgmtsys/login",
+    registerUrl: "https://hclko.allahabadhighcourt.in/advmgmtsys/registration",
+  });
+});
+
+// ── Diagnostics: dry-run a single POST and return raw response ──
+app.get("/api/advocate-dryrun", async (req, res) => {
+  try {
+    const { roll, bench = "lucknow", year } = req.query;
+    if (!roll) return res.status(400).json({ error: "missing ?roll=" });
+    const out = await dryRunAdvocate(bench, roll, year);
+    res.json(out);
+  } catch (err) {
+    res.status(500).json({ status: "error", error: err.message });
+  }
+});
+
+// ── Diagnostics: what URLs / formats does the backend try for a given roll number? ──
+app.get("/api/advocate-diag/:rollNumber", async (req, res) => {
+  try {
+    const { rollNumber } = req.params;
+    const variants = rollNumberVariants(rollNumber);
+    const currentYear = istNow().getFullYear();
+    res.json({
+      input: rollNumber,
+      variants,
+      allahabad: {
+        getUrl: `${CCMS_ALD_GET}/advocate-cases-roll-wise`,
+        postUrl: `${CCMS_ALD}/advocate-cases-roll-wise`,
+        fields: ["adv_roll", "case_year", "captchacode", "submit=Go"],
+        yearsToScan: Array.from({ length: 9 }, (_, i) => currentYear - i),
+      },
+      lucknow: {
+        getUrl: `${CCMS_LKO_GET}/advocate-cases-date-wise`,
+        postUrl: `${CCMS_LKO}/advocate-cases-date-wise`,
+        fields: ["adv_roll", "captchacode", "submit=Go"],
+      },
+      note: "CCMS uses the Advocate Roll Number (e.g. B/A2401/2019), not the Bar Council enrollment number (e.g. UP/2030/2018).",
+    });
+  } catch (err) {
+    res.status(500).json({ status: "error", error: err.message });
+  }
+});
+
+// ── Legal updates / news (merged Allahabad HC RSS feeds) ──
+app.get("/api/news", async (req, res) => {
+  try {
+    const result = await fetchLegalNews();
+    const limit = parseInt(req.query.limit, 10);
+    if (Number.isFinite(limit) && limit > 0) {
+      res.json({ ...result, items: result.items.slice(0, limit) });
+    } else {
+      res.json(result);
+    }
+  } catch (err) {
+    res.status(500).json({ status: "error", error: err.message });
+  }
+});
+
 // ── Live court view (for real-time ETA) ──
 app.get("/api/court-view", async (req, res) => {
   try {
diff --git a/index.html b/index.html
index d6ca6d9..103fe5c 100644
--- a/index.html
+++ b/index.html
@@ -2162,6 +2162,12 @@ const CourtAPI = {
     return this._fetch('/api/court-calendar');
   },
 
+  /** Fetch live Allahabad HC legal news (merged headlines + judgments RSS) */
+  async getNews(limit) {
+    const q = limit ? `?limit=${limit}` : '';
+    return this._fetch(`/api/news${q}`);
+  },
+
   /** Convert raw API case data to the app's Matter format */
   apiCaseToMatter(apiCase, index) {
     const id = 'live_' + (apiCase.caseType || 'X') + '_' + (apiCase.caseNo || index) + '_' + (apiCase.year || '');
@@ -2252,6 +2258,17 @@ const COURTS = [
   'Allahabad High Court (Lucknow Bench)',
 ];
 
+// Auto-detect bench from Advocate Roll Number prefix.
+// Convention at Allahabad HC: "A/..." = Principal Seat (Allahabad), "B/..." = Lucknow Bench.
+// Returns the matching court string from COURTS, or null if we can't tell.
+function suggestCourtFromRoll(rollNumber) {
+  if (!rollNumber) return null;
+  const s = String(rollNumber).trim().toUpperCase();
+  if (/^B\//.test(s)) return COURTS[1]; // Lucknow Bench
+  if (/^A\//.test(s)) return COURTS[0]; // Principal Seat
+  return null;
+}
+
 // ─────────────────────────────────────────────────────────────────────
 // CASE TYPE MAPPING - Allahabad High Court
 // ─────────────────────────────────────────────────────────────────────
@@ -2677,14 +2694,217 @@ const DEFAULT_INVOICES = [
   { id: 'inv4', subject: 'Retainer Fee - May 2026', total: 50000, date: '2026-05-01', matterId: null },
 ];
 
-const LEGAL_NEWS = [
-  { id: 'n1', title: 'Supreme Court upholds constitutional validity of new criminal laws', desc: 'The apex court dismissed a batch of petitions challenging BNS, BNSS and BSA.', time: '2 hours ago', url: 'https://main.sci.gov.in/supremecourt/judgements.htm' },
-  { id: 'n2', title: 'Allahabad HC issues guidelines on bail applications in POCSO cases', desc: 'New guidelines mandate consideration of specific factors.', time: '5 hours ago', url: 'https://www.allahabadhighcourt.in/jo.htm' },
-  { id: 'n3', title: 'E-filing mandatory for all High Courts from July 2026', desc: 'Supreme Court directed all High Courts to implement e-filing.', time: '1 day ago', url: 'https://www.allahabadhighcourt.in/efiling_21042021_landing.html' },
-  { id: 'n4', title: 'Bar Council of India announces revised rules for advocate registration', desc: 'Updated rules for enrollment and renewal of advocate licenses.', time: '2 days ago', url: 'https://www.barcouncilofindia.org' },
-  { id: 'n5', title: 'Lucknow Bench to start evening courts for commercial disputes', desc: 'Pilot program beginning June 2026 for expedited dispute resolution.', time: '3 days ago', url: 'https://www.allahabadhighcourt.in/calendar/itemWiseList.jsp?group=5' },
+// Fallback only — shown when the live RSS fetch is loading or has failed.
+// Real news comes from useLegalNews() → /api/news (Allahabad HC RSS feeds).
+const LEGAL_NEWS_FALLBACK = [
+  { id: 'n1', title: 'Loading latest legal updates from Allahabad High Court…', desc: 'Live RSS feeds (headlines + AFR/Landmark judgments) are being fetched. If this message persists, the backend may be offline.', time: 'just now', url: 'https://www.allahabadhighcourt.in/' },
 ];
 
+// Format an ISO date as "x hours ago" / "x days ago" for the news card.
+function relativeTime(iso) {
+  if (!iso) return '';
+  const t = Date.parse(iso);
+  if (isNaN(t)) return '';
+  const diff = Date.now() - t;
+  const min = Math.floor(diff / 60000);
+  if (min < 1) return 'just now';
+  if (min < 60) return `${min} min ago`;
+  const hr = Math.floor(min / 60);
+  if (hr < 24) return `${hr} hour${hr === 1 ? '' : 's'} ago`;
+  const d = Math.floor(hr / 24);
+  if (d < 30) return `${d} day${d === 1 ? '' : 's'} ago`;
+  const mo = Math.floor(d / 30);
+  if (mo < 12) return `${mo} month${mo === 1 ? '' : 's'} ago`;
+  return new Date(iso).toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' });
+}
+
+// Module-level cache so dashboard + Legal Updates page share one network call.
+let _legalNewsCache = null;
+let _legalNewsPromise = null;
+function fetchLegalNewsOnce() {
+  if (_legalNewsCache) return Promise.resolve(_legalNewsCache);
+  if (_legalNewsPromise) return _legalNewsPromise;
+  _legalNewsPromise = CourtAPI.getNews(40)
+    .then(res => {
+      if (res && res.status === 'success' && Array.isArray(res.items)) {
+        _legalNewsCache = res.items.map(it => ({
+          id: it.id,
+          title: it.title,
+          desc: it.description || '',
+          time: relativeTime(it.pubDate) || (it.pubDateRaw || ''),
+          url: it.link || '#',
+          source: it.source, // 'headlines' | 'judgments'
+        }));
+        return _legalNewsCache;
+      }
+      return LEGAL_NEWS_FALLBACK;
+    })
+    .catch(() => LEGAL_NEWS_FALLBACK)
+    .finally(() => { _legalNewsPromise = null; });
+  return _legalNewsPromise;
+}
+
+// React hook for components that render legal news.
+function useLegalNews() {
+  const [items, setItems] = useState(_legalNewsCache || LEGAL_NEWS_FALLBACK);
+  const [loading, setLoading] = useState(!_legalNewsCache);
+  useEffect(() => {
+    let alive = true;
+    fetchLegalNewsOnce().then(list => {
+      if (!alive) return;
+      setItems(list);
+      setLoading(false);
+    });
+    return () => { alive = false; };
+  }, []);
+  return { items, loading };
+}
+
+// Live Court View — replaces the unworkable iframe of courtview2.allahabadhighcourt.in.
+// The court page sends X-Frame-Options: SAMEORIGIN, so iframe embedding is blocked by every browser.
+// Instead we fetch /api/court-view (which scrapes the same page server-side) and render natively.
+function LiveCourtViewPanel({ bench, externalUrl, refreshSignal }) {
+  const [data, setData] = useState(null);
+  const [loading, setLoading] = useState(false);
+  const [error, setError] = useState(null);
+  const [refreshedAt, setRefreshedAt] = useState(null);
+
+  useEffect(() => {
+    let alive = true;
+    let timer = null;
+    const load = async (showSpinner) => {
+      if (showSpinner) setLoading(true);
+      try {
+        const res = await CourtAPI.getCourtView(bench);
+        if (!alive) return;
+        setData(res);
+        setError(null);
+        setRefreshedAt(new Date());
+      } catch (e) {
+        if (!alive) return;
+        setError(e.message || 'Failed to fetch court view');
+      } finally {
+        if (alive) setLoading(false);
+      }
+    };
+    load(true);
+    timer = setInterval(() => load(false), 30000); // backend caches for 30s
+    return () => { alive = false; if (timer) clearInterval(timer); };
+  }, [bench, refreshSignal]);
+
+  const courts = (data && data.courts) || [];
+  const inSession = courts.filter(c => c.status === 'in_session');
+  const notInSession = courts.filter(c => c.status !== 'in_session');
+
+  return (
+    <div style={{padding:'12px 16px'}}>
+      <div style={{display:'flex', justifyContent:'space-between', alignItems:'center', marginBottom:10, flexWrap:'wrap', gap:8}}>
+        <div style={{fontSize:12, color:'var(--text-muted)'}}>
+          {loading && !data && 'Loading live court view…'}
+          {data && (
+            <>
+              <strong style={{color:'var(--text-primary)'}}>{courts.length}</strong> courts ·
+              <strong style={{color:'var(--success)', marginLeft:6}}>{inSession.length}</strong> in session ·
+              <strong style={{color:'var(--text-muted)', marginLeft:6}}>{notInSession.length}</strong> idle
+              {refreshedAt && <span style={{marginLeft:10, fontFamily:'monospace', fontSize:11}}>· refreshed {refreshedAt.toLocaleTimeString('en-IN', { hour12: false })}</span>}
+            </>
+          )}
+        </div>
+        <div style={{display:'flex', gap:8}}>
+          <a href={externalUrl} target="_blank" rel="noopener" style={{fontSize:11, color:'var(--primary)', textDecoration:'none'}}>Open official page ↗</a>
+        </div>
+      </div>
+
+      {error && (
+        <div style={{padding:'10px 14px', borderRadius:'var(--radius)', background:'var(--danger-bg)', color:'var(--danger)', fontSize:13, marginBottom:10}}>
+          Couldn't reach the live court view backend: {error}
+        </div>
+      )}
+
+      {inSession.length > 0 && (
+        <div style={{overflowX:'auto', border:'1px solid var(--border)', borderRadius:'var(--radius)'}}>
+          <table style={{width:'100%', borderCollapse:'collapse', fontSize:12}}>
+            <thead>
+              <tr style={{background:'var(--bg-elevated)', textAlign:'left'}}>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Court</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Item</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Case</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Title</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Petitioner's Counsel</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Respondent's Counsel</th>
+                <th style={{padding:'8px 10px', borderBottom:'1px solid var(--border)'}}>Progress</th>
+              </tr>
+            </thead>
+            <tbody>
+              {inSession.map((c, i) => (
+                <tr key={i} style={{borderBottom:'1px solid var(--border-light)', background: i % 2 ? 'var(--bg-card)' : 'transparent'}}>
+                  <td style={{padding:'8px 10px', fontWeight:700, color:'var(--primary)'}}>{c.courtNo}</td>
+                  <td style={{padding:'8px 10px', fontFamily:'monospace'}}>{c.currentItem}</td>
+                  <td style={{padding:'8px 10px', fontFamily:'monospace', whiteSpace:'nowrap'}}>{c.caseRef || c.caseBeingHeard || '—'}</td>
+                  <td style={{padding:'8px 10px', maxWidth:280}}>{c.title || '—'}</td>
+                  <td style={{padding:'8px 10px', maxWidth:200, fontSize:11, color:'var(--text-secondary)'}}>{c.petitionerCounsel || '—'}</td>
+                  <td style={{padding:'8px 10px', maxWidth:200, fontSize:11, color:'var(--text-secondary)'}}>{c.respondentCounsel || '—'}</td>
+                  <td style={{padding:'8px 10px', fontSize:11}}>{c.progress || c.causelistType || ''}</td>
+                </tr>
+              ))}
+            </tbody>
+          </table>
+        </div>
+      )}
+
+      {data && inSession.length === 0 && (
+        <div style={{padding:'30px 20px', textAlign:'center', background:'var(--bg-card)', border:'1px dashed var(--border)', borderRadius:'var(--radius)'}}>
+          <div style={{fontSize:36, marginBottom:8, opacity:0.5}}>{Icons.monitor}</div>
+          <div style={{fontWeight:700, marginBottom:4}}>No courts in session right now</div>
+          <div style={{fontSize:12, color:'var(--text-muted)', maxWidth:420, margin:'0 auto'}}>
+            All {courts.length} court rooms show "not in session". This is normal outside hearing hours, on weekends, holidays, or during court vacations. Data refreshes every 30 seconds.
+          </div>
+        </div>
+      )}
+
+      {data && notInSession.length > 0 && (
+        <details style={{marginTop:12}}>
+          <summary style={{cursor:'pointer', fontSize:12, color:'var(--text-muted)'}}>Show {notInSession.length} idle court room{notInSession.length === 1 ? '' : 's'}</summary>
+          <div style={{display:'flex', flexWrap:'wrap', gap:6, marginTop:8}}>
+            {notInSession.map((c, i) => (
+              <span key={i} style={{padding:'3px 8px', fontSize:11, background:'var(--bg-elevated)', border:'1px solid var(--border)', borderRadius:'var(--radius-full)', color:'var(--text-muted)'}}>
+                {c.courtNo}
+              </span>
+            ))}
+          </div>
+        </details>
+      )}
+    </div>
+  );
+}
+
+// Compact Legal Updates card used on the dashboard.
+function LegalUpdatesCard({ navigate, limit = 4 }) {
+  const { items, loading } = useLegalNews();
+  return (
+    <div className="card">
+      <div className="card-header">
+        <span className="card-title">Legal Updates</span>
+        <button className="btn btn-sm btn-ghost" onClick={()=>navigate('legalupdates')}>View All</button>
+      </div>
+      <div className="card-body" style={{padding:'8px 20px'}}>
+        {loading && items === LEGAL_NEWS_FALLBACK && (
+          <div style={{fontSize:12, color:'var(--text-muted)', padding:8, textAlign:'center'}}>Loading live updates…</div>
+        )}
+        {items.slice(0, limit).map(n => (
+          <div key={n.id} className="news-card" style={{cursor: n.url && n.url !== '#' ? 'pointer' : 'default'}} onClick={() => { if (n.url && n.url !== '#') window.open(n.url, '_blank', 'noopener'); }}>
+            <div className="news-thumb">{Icons.newspaper}</div>
+            <div className="news-content">
+              <div className="news-title">{n.title}</div>
+              <div className="news-time">{n.time}{n.source ? ` · ${n.source === 'judgments' ? 'Judgment' : 'Headline'}` : ''}</div>
+            </div>
+          </div>
+        ))}
+      </div>
+    </div>
+  );
+}
+
 const COURT_RESOURCES = {
   judgments: {
     title: 'Judgments & Orders',
@@ -3301,10 +3521,32 @@ function LoginPage({ onLogin }) {
               </div>
               <div className="form-group">
                 <label className="form-label">Advocate ID / AOR No. <span style={{fontSize:11, color:'var(--text-muted)', fontWeight:400}}>(used for case lookup)</span></label>
-                <input className="form-input" placeholder="e.g. B/A2401/2019 or AOR/5678" value={form.rollNo} onChange={e => setForm({...form, rollNo: e.target.value})} />
+                <input className="form-input" placeholder="e.g. B/A2401/2019 or AOR/5678" value={form.rollNo} onChange={e => {
+                  const rollNo = e.target.value;
+                  // Auto-suggest the bench from the roll prefix (A/ = Allahabad, B/ = Lucknow Bench).
+                  const suggested = suggestCourtFromRoll(rollNo);
+                  setForm(prev => ({
+                    ...prev,
+                    rollNo,
+                    // Only override the court if the user hasn't manually changed it yet,
+                    // or if their current value is the default.
+                    court: suggested && (prev.court === COURTS[0] || !prev.court) ? suggested : prev.court,
+                  }));
+                }} />
+                {form.rollNo && (() => {
+                  const s = suggestCourtFromRoll(form.rollNo);
+                  if (s && s !== form.court) {
+                    return <div style={{fontSize:11, color:'var(--primary)', marginTop:4}}>
+                      Hint: roll numbers starting with {form.rollNo.trim()[0].toUpperCase()}/ are typically <strong>{s}</strong>.
+                      <button type="button" style={{marginLeft:6, fontSize:11, padding:'2px 8px', border:'1px solid var(--primary)', background:'transparent', color:'var(--primary)', borderRadius:4, cursor:'pointer'}}
+                        onClick={() => setForm(prev => ({...prev, court: s}))}>Use {s.includes('Lucknow') ? 'Lucknow Bench' : 'Principal Seat'}</button>
+                    </div>;
+                  }
+                  return null;
+                })()}
               </div>
               <div className="form-group">
-                <label className="form-label">Select Court</label>
+                <label className="form-label">Select Court / Bench <span style={{fontSize:11, color:'var(--text-muted)', fontWeight:400}}>(determines which bench is queried first)</span></label>
                 <select className="form-select" value={form.court} onChange={e => setForm({...form, court: e.target.value})}>
                   {COURTS.map(c => <option key={c} value={c}>{c}</option>)}
                 </select>
@@ -3679,8 +3921,9 @@ function DashboardPage() {
     },
     causelists: {
       label: 'Cause Lists', labelHi: 'वाद सूची', icon: Icons.fileText,
-      urlAld: 'https://www.allahabadhighcourt.in/causelist/causelist_ald.htm',
-      urlLko: 'https://www.allahabadhighcourt.in/causelist/causelist_lko.htm',
+      // The old /causelist/causelist_*.htm URLs returned 404. Live cause list now lives on the CCMS app.
+      urlAld: 'https://www.allahabadhighcourt.in/apps/status_ccms/index.php/causelist',
+      urlLko: 'https://hclko.allahabadhighcourt.in/status/index.php/cause-list',
       desc: "Today's cause list — cases scheduled for hearing",
     },
     efiling: {
@@ -3829,20 +4072,26 @@ function DashboardPage() {
             <span>{currentPanel.desc} — {liveBench === 'lucknow' ? 'Lucknow Bench' : 'Allahabad (Principal Seat)'}</span>
             <span style={{fontFamily:'monospace', fontSize:10, opacity:0.6}}>{currentUrl.replace('https://',''). split('/').slice(0,2).join('/')}</span>
           </div>
-          <div style={{padding:'24px 20px', textAlign:'center', minHeight:200, display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', gap:16, background:'var(--bg-card)'}}>
-            <div style={{width:64, height:64, borderRadius:'50%', background:'var(--primary-bg)', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--primary)'}}>{currentPanel.icon}</div>
-            <div>
-              <div style={{fontWeight:700, fontSize:16, marginBottom:4}}>{currentPanel.label}</div>
-              <div style={{fontSize:13, color:'var(--text-secondary)', maxWidth:400, margin:'0 auto'}}>{currentPanel.desc}</div>
-            </div>
-            <div style={{display:'flex', gap:8, flexWrap:'wrap', justifyContent:'center'}}>
-              <button className="btn btn-primary" onClick={() => window.open(currentUrl, '_blank')}>{Icons.externalLink} Open {currentPanel.label}</button>
-              <button className="btn btn-outline" onClick={() => openCourtPopup(currentUrl, currentPanel.label)}>{Icons.monitor} Open in Popup Window</button>
-            </div>
-            <div style={{fontSize:11, color:'var(--text-muted)', marginTop:8, maxWidth:450}}>
-              Court websites restrict embedding for security. Click the button above to open the live {currentPanel.label.toLowerCase()} directly from the Allahabad High Court website.
+          {livePanel === 'courtview' ? (
+            // Native live render — replaces the unworkable iframe.
+            // /api/court-view scrapes courtview2.allahabadhighcourt.in server-side and returns JSON.
+            <LiveCourtViewPanel bench={liveBench} externalUrl={currentUrl} refreshSignal={iframeKey} />
+          ) : (
+            <div style={{padding:'24px 20px', textAlign:'center', minHeight:200, display:'flex', flexDirection:'column', alignItems:'center', justifyContent:'center', gap:16, background:'var(--bg-card)'}}>
+              <div style={{width:64, height:64, borderRadius:'50%', background:'var(--primary-bg)', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--primary)'}}>{currentPanel.icon}</div>
+              <div>
+                <div style={{fontWeight:700, fontSize:16, marginBottom:4}}>{currentPanel.label}</div>
+                <div style={{fontSize:13, color:'var(--text-secondary)', maxWidth:400, margin:'0 auto'}}>{currentPanel.desc}</div>
+              </div>
+              <div style={{display:'flex', gap:8, flexWrap:'wrap', justifyContent:'center'}}>
+                <button className="btn btn-primary" onClick={() => window.open(currentUrl, '_blank')}>{Icons.externalLink} Open {currentPanel.label}</button>
+                <button className="btn btn-outline" onClick={() => openCourtPopup(currentUrl, currentPanel.label)}>{Icons.monitor} Open in Popup Window</button>
+              </div>
+              <div style={{fontSize:11, color:'var(--text-muted)', marginTop:8, maxWidth:450}}>
+                Court websites restrict embedding for security. Click the button above to open the live {currentPanel.label.toLowerCase()} directly from the Allahabad High Court website.
+              </div>
             </div>
-          </div>
+          )}
         </div>
       </div>
 
@@ -3963,24 +4212,8 @@ function DashboardPage() {
           </div>
         </div>
         <div>
-          {/* Legal Updates */}
-          <div className="card">
-            <div className="card-header">
-              <span className="card-title">Legal Updates</span>
-              <button className="btn btn-sm btn-ghost" onClick={()=>navigate('legalupdates')}>View All</button>
-            </div>
-            <div className="card-body" style={{padding:'8px 20px'}}>
-              {LEGAL_NEWS.slice(0,4).map(n => (
-                <div key={n.id} className="news-card">
-                  <div className="news-thumb">{Icons.newspaper}</div>
-                  <div className="news-content">
-                    <div className="news-title">{n.title}</div>
-                    <div className="news-time">{n.time}</div>
-                  </div>
-                </div>
-              ))}
-            </div>
-          </div>
+          {/* Legal Updates — live from Allahabad HC RSS feeds (was previously a hardcoded 5-item array) */}
+          <LegalUpdatesCard navigate={navigate} limit={4} />
 
           {/* Quick Actions */}
           <div className="card" style={{marginTop:16}}>
@@ -4036,6 +4269,74 @@ function DashboardPage() {
 // ─────────────────────────────────────────────────────────────────────
 // MATTERS PAGE
 // ─────────────────────────────────────────────────────────────────────
+// Banner explaining the live-fetch state on the Matters page.
+// Replaces the silent display of demo matters with a clear message about what's happening.
+function LiveFetchBanner() {
+  const { liveFetchStatus, loadingLiveCases, user, matters } = useContext(AppContext);
+  if (!user) return null;
+  // Detect whether the visible matters are the canned demo set (Rajesh Kumar Mishra etc.)
+  const isDemoData = matters?.length > 0 && matters[0]?.id === 'm1' && matters[0]?.title?.includes('Rajesh Kumar Mishra');
+  const status = liveFetchStatus || { kind: 'idle' };
+
+  if (isDemoData && (user.barCouncil || user.rollNo)) {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--warning-bg)', color:'var(--warning)', border:'1px solid var(--warning)', fontSize:13, display:'flex', gap:10, alignItems:'flex-start'}}>
+        <div style={{fontWeight:700}}>⚠ Sample data shown.</div>
+        <div>
+          You're seeing 3 demo matters (Rajesh Kumar Mishra etc.), not your real cases.
+          {loadingLiveCases ? ' Fetching your live matters now…' : ' Live fetch in progress — refresh the page in a moment.'}
+        </div>
+      </div>
+    );
+  }
+
+  if (status.kind === 'loading' || loadingLiveCases) {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--primary-bg)', color:'var(--primary)', fontSize:13}}>
+        Fetching your live matters from Allahabad HC CCMS… The court portal uses CAPTCHAs and can take 30-60 seconds per bench on the first call.
+      </div>
+    );
+  }
+  if (status.kind === 'upstream_maintenance') {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--warning-bg)', color:'var(--warning)', border:'1px solid var(--warning)', fontSize:13}}>
+        <strong>Allahabad HC CCMS (Lucknow Bench) is in maintenance.</strong>{' '}
+        {status.message}
+      </div>
+    );
+  }
+  if (status.kind === 'no_cases') {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--info-bg)', color:'var(--info)', border:'1px solid var(--info)', fontSize:13}}>
+        <strong>No live cases found</strong> for roll number {status.lookupId ? <code>{status.lookupId}</code> : ''}. {status.message}
+        {status.benchStatus && (
+          <div style={{fontSize:11, marginTop:6, opacity:0.85}}>
+            Allahabad: {status.benchStatus.allahabad?.status}{status.benchStatus.allahabad?.count > 0 ? ` (${status.benchStatus.allahabad.count})` : ''} ·
+            Lucknow: {status.benchStatus.lucknow?.status}{status.benchStatus.lucknow?.count > 0 ? ` (${status.benchStatus.lucknow.count})` : ''}
+          </div>
+        )}
+      </div>
+    );
+  }
+  if (status.kind === 'backend_down') {
+    return (
+      <div style={{padding:'12px 16px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--danger-bg)', color:'var(--danger)', border:'1px solid var(--danger)', fontSize:13}}>
+        Backend API at <code>legaldesk-api.onrender.com</code> is unreachable. Free Render tier sleeps after 15 min — first request can take 30s. Wait, then refresh.
+      </div>
+    );
+  }
+  if (status.kind === 'success' && status.benchStatus) {
+    return (
+      <div style={{padding:'10px 14px', marginBottom:16, borderRadius:'var(--radius)', background:'var(--success-bg)', color:'var(--success)', fontSize:12}}>
+        Live: <strong>{status.allahabadCount}</strong> Allahabad ·
+        <strong style={{marginLeft:6}}>{status.lucknowCount}</strong> Lucknow Bench
+        {status.benchStatus.lucknow?.note && <div style={{fontSize:11, opacity:0.85, marginTop:4}}>{status.benchStatus.lucknow.note}</div>}
+      </div>
+    );
+  }
+  return null;
+}
+
 function MattersPage() {
   const { matters, setMatters, navigate, setSelectedMatter, addToast } = useContext(AppContext);
   const [tab, setTab] = useState('pending');
@@ -4092,6 +4393,8 @@ function MattersPage() {
         <button className="btn btn-primary" onClick={()=>setShowModal(true)}>{Icons.plus} Create New</button>
       </div>
 
+      <LiveFetchBanner />
+
       <div className="card">
         <div className="card-header" style={{flexWrap:'wrap'}}>
           <div className="tabs">
@@ -5804,7 +6107,10 @@ function DisplayBoardPage() {
                     {Icons.externalLink} Open Live Display Board
                   </button>
                   <button className="btn btn-outline" onClick={() => {
-                    openCourtPopup('https://www.allahabadhighcourt.in/causelist/causelist_' + (liveBench==='lucknow'?'lko':'ald') + '.htm', 'CauseList');
+                    const causelistUrl = liveBench==='lucknow'
+                      ? 'https://hclko.allahabadhighcourt.in/status/index.php/cause-list'
+                      : 'https://www.allahabadhighcourt.in/apps/status_ccms/index.php/causelist';
+                    openCourtPopup(causelistUrl, 'CauseList');
                     addToast('Cause List page opened', 'info');
                   }}>
                     {Icons.fileText} Today's Cause List
@@ -6002,14 +6308,28 @@ function DisplayBoardPage() {
 // LEGAL UPDATES PAGE
 // ─────────────────────────────────────────────────────────────────────
 function LegalUpdatesPage() {
+  const { items, loading } = useLegalNews();
+  const [filter, setFilter] = useState('all'); // all | headlines | judgments
+  const filtered = filter === 'all' ? items : items.filter(n => n.source === filter);
   return (
     <div className="content">
-      <div className="page-header"><h2 className="page-title">Legal Updates</h2></div>
+      <div className="page-header">
+        <h2 className="page-title">Legal Updates</h2>
+        <div style={{display:'flex', gap:8}}>
+          <button className={`btn btn-sm ${filter==='all'?'btn-primary':'btn-outline'}`} onClick={()=>setFilter('all')}>All ({items.length})</button>
+          <button className={`btn btn-sm ${filter==='headlines'?'btn-primary':'btn-outline'}`} onClick={()=>setFilter('headlines')}>Headlines</button>
+          <button className={`btn btn-sm ${filter==='judgments'?'btn-primary':'btn-outline'}`} onClick={()=>setFilter('judgments')}>Judgments</button>
+        </div>
+      </div>
+      {loading && items === LEGAL_NEWS_FALLBACK && (
+        <div style={{padding:20, color:'var(--text-muted)'}}>Loading live updates from Allahabad HC RSS feeds…</div>
+      )}
       <div style={{display:'grid', gridTemplateColumns:'repeat(auto-fill, minmax(340px, 1fr))', gap:16}}>
-        {LEGAL_NEWS.map(n => (
-          <div key={n.id} className="card" style={{cursor:'pointer', transition:'var(--transition)'}} onClick={() => window.open(n.url, '_blank', 'noopener')}>
-            <div style={{height:140, background:'var(--primary-bg)', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--primary)'}}>
+        {filtered.map(n => (
+          <div key={n.id} className="card" style={{cursor: n.url && n.url !== '#' ? 'pointer' : 'default', transition:'var(--transition)'}} onClick={() => { if (n.url && n.url !== '#') window.open(n.url, '_blank', 'noopener'); }}>
+            <div style={{height:140, background:'var(--primary-bg)', display:'flex', alignItems:'center', justifyContent:'center', color:'var(--primary)', position:'relative'}}>
               <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" strokeLinejoin="round"><path d="M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 1-2 2Zm0 0a2 2 0 0 1-2-2v-9c0-1.1.9-2 2-2h2"/><path d="M18 14h-8M15 18h-5M10 6h8v4h-8V6Z"/></svg>
+              {n.source && <span style={{position:'absolute', top:8, right:8, background:'rgba(255,255,255,0.9)', color:'var(--primary)', fontSize:10, fontWeight:700, padding:'2px 8px', borderRadius:10, textTransform:'uppercase'}}>{n.source === 'judgments' ? 'Judgment' : 'Headline'}</span>}
             </div>
             <div className="card-body">
               <div style={{fontWeight:700, fontSize:15, marginBottom:6, lineHeight:1.4}}>{n.title}</div>
@@ -6708,6 +7028,41 @@ function SettingsPage() {
   const { user, setUser, theme, setTheme, addToast, teamMembers, setTeamMembers } = useContext(AppContext);
   const [newMemberName, setNewMemberName] = useState('');
   const [newMemberPhone, setNewMemberPhone] = useState('');
+  // Editable bench/court — defaults to the suggested bench based on the roll number prefix.
+  const suggestedCourt = suggestCourtFromRoll(user?.rollNo);
+  const [editingCourt, setEditingCourt] = useState(false);
+  const [chosenCourt, setChosenCourt] = useState(user?.court || suggestedCourt || COURTS[0]);
+  // advmgmtsys creds stored ONLY in localStorage (not in our DB; not transmitted to backend
+  // until the user explicitly clicks Connect). When implemented in a later sprint, the backend
+  // will use these creds once per fetch and discard them.
+  const [advmgmtCreds, setAdvmgmtCreds] = useState(() => LS.get('advmgmtsys_creds', { username: user?.rollNo || '', password: '' }));
+  const [showAdvmgmtForm, setShowAdvmgmtForm] = useState(false);
+
+  const saveCourt = () => {
+    const updated = { ...user, court: chosenCourt };
+    setUser(updated);
+    LS.set('user', updated);
+    if (user?.uid && typeof FirestoreService !== 'undefined' && FirestoreService.saveProfile) {
+      FirestoreService.saveProfile(user.uid, updated).catch(() => {});
+    }
+    setEditingCourt(false);
+    addToast('Court / bench updated', 'success');
+  };
+
+  const saveAdvmgmtCreds = () => {
+    if (!advmgmtCreds.username.trim() || !advmgmtCreds.password.trim()) {
+      addToast('Enter both username and password', 'error'); return;
+    }
+    LS.set('advmgmtsys_creds', advmgmtCreds);
+    setShowAdvmgmtForm(false);
+    addToast('Credentials saved locally. (Backend sync not yet wired — coming next sprint.)', 'info');
+  };
+
+  const clearAdvmgmtCreds = () => {
+    LS.remove('advmgmtsys_creds');
+    setAdvmgmtCreds({ username: user?.rollNo || '', password: '' });
+    addToast('Advmgmtsys credentials cleared from this browser', 'info');
+  };
 
   const handleLogout = async () => {
     // Cache profile for quick re-login (skip verification on next sign-in)
@@ -6767,8 +7122,72 @@ function SettingsPage() {
             <input className="form-input" value={user?.rollNo || ''} readOnly />
           </div>
           <div className="form-group">
-            <label className="form-label">Court</label>
-            <input className="form-input" value={user?.court || ''} readOnly />
+            <label className="form-label">Court / Bench</label>
+            {!editingCourt ? (
+              <div style={{display:'flex', gap:8, alignItems:'center'}}>
+                <input className="form-input" value={user?.court || ''} readOnly style={{flex:1}} />
+                <button type="button" className="btn btn-outline btn-sm" onClick={() => setEditingCourt(true)}>Change</button>
+              </div>
+            ) : (
+              <div style={{display:'flex', gap:8, alignItems:'center'}}>
+                <select className="form-select" value={chosenCourt} onChange={e => setChosenCourt(e.target.value)} style={{flex:1}}>
+                  {COURTS.map(c => <option key={c} value={c}>{c}</option>)}
+                </select>
+                <button type="button" className="btn btn-primary btn-sm" onClick={saveCourt}>Save</button>
+                <button type="button" className="btn btn-ghost btn-sm" onClick={() => { setChosenCourt(user?.court || COURTS[0]); setEditingCourt(false); }}>Cancel</button>
+              </div>
+            )}
+            {suggestedCourt && suggestedCourt !== user?.court && (
+              <div style={{fontSize:11, color:'var(--warning)', marginTop:6}}>
+                Heads up: your roll number <code>{user?.rollNo}</code> starts with <strong>{user?.rollNo?.[0]?.toUpperCase()}/</strong> which usually means <strong>{suggestedCourt}</strong>.
+                {!editingCourt && (
+                  <button type="button" style={{marginLeft:6, fontSize:11, padding:'2px 8px', border:'1px solid var(--warning)', background:'transparent', color:'var(--warning)', borderRadius:4, cursor:'pointer'}}
+                    onClick={() => { setChosenCourt(suggestedCourt); setEditingCourt(true); }}>Switch to {suggestedCourt.includes('Lucknow') ? 'Lucknow Bench' : 'Principal Seat'}</button>
+                )}
+              </div>
+            )}
+          </div>
+
+          {/* ─── CCMS / advmgmtsys credential connection (alpha) ─── */}
+          <div style={{borderTop:'1px solid var(--border)', paddingTop:20, marginTop:20}}>
+            <h3 style={{fontSize:15, fontWeight:700, marginBottom:8}}>{Icons.lock || '🔒'} Court Portal Connection (Alpha)</h3>
+            <p style={{fontSize:12, color:'var(--text-secondary)', marginBottom:12, lineHeight:1.55}}>
+              Public CCMS gives only listed-today / per-year case lookups. To pull your <strong>full case history</strong>{' '}
+              (the way MyMunshi does), LegalDesk needs your <strong>Allahabad HC Advocate Management System (advmgmtsys)</strong> login.
+              Register at <a href="https://hclko.allahabadhighcourt.in/advmgmtsys/registration" target="_blank" rel="noopener" style={{color:'var(--primary)'}}>advmgmtsys/registration</a> if you haven't already.
+            </p>
+            <p style={{fontSize:11, color:'var(--text-muted)', marginBottom:12, lineHeight:1.55}}>
+              <strong>Privacy:</strong> Your password is stored only in <em>this browser</em>. The backend uses it once per scheduled fetch and never persists it.
+              The end-to-end fetcher ships next sprint; for now you can pre-fill your username so it's ready to go.
+            </p>
+            {!showAdvmgmtForm && !LS.get('advmgmtsys_creds') && (
+              <button type="button" className="btn btn-outline btn-sm" onClick={() => setShowAdvmgmtForm(true)}>
+                {Icons.link || '🔗'} Connect CCMS account
+              </button>
+            )}
+            {!showAdvmgmtForm && LS.get('advmgmtsys_creds') && (
+              <div style={{display:'flex', gap:8, alignItems:'center'}}>
+                <span style={{fontSize:12, color:'var(--success)'}}>✓ Credentials saved for <code>{LS.get('advmgmtsys_creds')?.username}</code></span>
+                <button type="button" className="btn btn-ghost btn-sm" onClick={() => setShowAdvmgmtForm(true)} style={{marginLeft:'auto'}}>Edit</button>
+                <button type="button" className="btn btn-ghost btn-sm" style={{color:'var(--danger)'}} onClick={clearAdvmgmtCreds}>Clear</button>
+              </div>
+            )}
+            {showAdvmgmtForm && (
+              <div style={{background:'var(--bg-input)', padding:14, borderRadius:'var(--radius)', marginTop:8}}>
+                <div className="form-group" style={{marginBottom:10}}>
+                  <label className="form-label">Username (Advocate Roll, e.g. B/A2401/2019)</label>
+                  <input className="form-input" value={advmgmtCreds.username} onChange={e => setAdvmgmtCreds({...advmgmtCreds, username: e.target.value})} placeholder="B/A2401/2019" />
+                </div>
+                <div className="form-group" style={{marginBottom:10}}>
+                  <label className="form-label">Password</label>
+                  <input className="form-input" type="password" value={advmgmtCreds.password} onChange={e => setAdvmgmtCreds({...advmgmtCreds, password: e.target.value})} placeholder="Your advmgmtsys password" />
+                </div>
+                <div style={{display:'flex', gap:8}}>
+                  <button type="button" className="btn btn-primary btn-sm" onClick={saveAdvmgmtCreds}>Save in browser</button>
+                  <button type="button" className="btn btn-ghost btn-sm" onClick={() => setShowAdvmgmtForm(false)}>Cancel</button>
+                </div>
+              </div>
+            )}
           </div>
 
           <div style={{borderTop:'1px solid var(--border)', paddingTop:20, marginTop:20}}>
@@ -7481,7 +7900,19 @@ function App() {
   const [theme, setThemeState] = useState(() => LS.get('theme', 'light'));
   const [currentPage, setCurrentPage] = useState(() => LS.get('currentPage', 'dashboard'));
   const [sidebarOpen, setSidebarOpen] = useState(false);
-  const [matters, setMattersState] = useState(() => LS.get('matters', DEFAULT_MATTERS));
+  // Matters are loaded fresh per advocate. The DEFAULT_MATTERS sample data is ONLY shown
+  // on first run with no user and no prior data — once a real advocate signs in, their
+  // matters come from /api/advocate-cases-all and the demo data is replaced (or cleared
+  // entirely if the upstream genuinely has nothing for that roll number).
+  const [matters, setMattersState] = useState(() => {
+    const stored = LS.get('matters', null);
+    if (stored && Array.isArray(stored)) return stored;
+    const u = LS.get('user', null);
+    // Once a real user is signed in, don't seed demo matters at all.
+    return u && (u.barCouncil || u.rollNo) ? [] : DEFAULT_MATTERS;
+  });
+  // Status of the last live fetch so the UI can distinguish demo vs empty vs maintenance.
+  const [liveFetchStatus, setLiveFetchStatus] = useState({ kind: 'idle', message: '' });
   const [tasks, setTasksState] = useState(() => LS.get('tasks', DEFAULT_TASKS));
   const [invoices, setInvoicesState] = useState(() => LS.get('invoices', DEFAULT_INVOICES));
   const [calendarTasks, setCalendarTasksState] = useState(() => LS.get('calendarTasks', []));
@@ -7664,21 +8095,22 @@ function App() {
       // Check if backend is reachable
       const health = await CourtAPI.healthCheck();
       if (health.status !== 'ok') {
-        console.warn('[App] Backend API not reachable, using cached/demo data');
+        console.warn('[App] Backend API not reachable');
         setBackendOnline(false);
+        setLiveFetchStatus({ kind: 'backend_down', message: 'Backend is unreachable — cached data shown.' });
         return;
       }
       setBackendOnline(true);
       setLoadingLiveCases(true);
+      setLiveFetchStatus({ kind: 'loading', message: 'Fetching live matters from Allahabad HC CCMS…' });
 
       try {
-        // Try rollNo (Advocate ID) first since CCMS uses court-specific roll numbers,
-        // then fall back to barCouncil enrollment number
+        // Try roll number first (Advocate ID format the CCMS form expects),
+        // then fall back to barCouncil enrollment.
         const lookupId = user.rollNo || user.barCouncil;
         let result = await CourtAPI.getAdvocateCases(lookupId);
         if (cancelled) return;
 
-        // If rollNo didn't find cases and barCouncil is different, try that too
         if ((!result || result.status !== 'success' || result.cases?.length === 0)
             && user.rollNo && user.barCouncil && user.rollNo !== user.barCouncil) {
           console.log('[App] No cases with rollNo, trying barCouncil:', user.barCouncil);
@@ -7688,26 +8120,60 @@ function App() {
           }
         }
 
-        if (result.status === 'success' && result.cases.length > 0) {
-          // Convert API cases to app matter format
-          const liveCases = result.cases.map((c, i) => CourtAPI.apiCaseToMatter(c, i));
+        // Cases come back as either matters that already existed (synced via live id prefix)
+        // or as new ones. Either way, we keep manually-added matters and replace the live ones.
+        const existingMatters = LS.get('matters', []) || [];
+        const localOnlyMatters = existingMatters.filter(m =>
+          m && !m._liveData && !(typeof m.id === 'string' && m.id.startsWith('live_'))
+        );
 
-          // Merge with any local-only cases (manually added ones)
-          const existingMatters = LS.get('matters', []);
-          const localOnlyMatters = existingMatters.filter(m => !m._liveData);
+        if (result && result.status === 'success' && result.cases?.length > 0) {
+          const liveCases = result.cases.map((c, i) => {
+            const m = CourtAPI.apiCaseToMatter(c, i);
+            m._liveData = true;
+            return m;
+          });
           const merged = [...liveCases, ...localOnlyMatters];
-
           setMattersState(merged);
           LS.set('matters', merged);
           LS.set('matters_lastSync', Date.now());
+          setLiveFetchStatus({
+            kind: 'success',
+            message: `Loaded ${liveCases.length} live case(s).`,
+            allahabadCount: result.allahabadCases || 0,
+            lucknowCount: result.lucknowCases || 0,
+            benchStatus: result.benchStatus,
+          });
           addToast(`Loaded ${liveCases.length} live case(s) from Allahabad HC`, 'success');
-        } else if (result.status === 'no_cases_found') {
-          addToast('No cases found for your enrollment. You can add cases manually.', 'info');
-        } else if (result.status === 'captcha_failed') {
-          addToast('Court portal CAPTCHA failed. Using cached data. Will retry automatically.', 'info');
+        } else if (result && result.status === 'upstream_maintenance') {
+          // Don't seed demo matters — show real empty state with clear maintenance reason.
+          const onlyLocal = localOnlyMatters;
+          setMattersState(onlyLocal);
+          LS.set('matters', onlyLocal);
+          LS.set('matters_lastSync', Date.now());
+          setLiveFetchStatus({
+            kind: 'upstream_maintenance',
+            message: 'Allahabad HC CCMS (Lucknow Bench) is currently in maintenance. Live matters will appear when the court portal is back online — typically within a few hours.',
+            benchStatus: result.benchStatus,
+          });
+          addToast('Court portal is in maintenance. Will retry automatically.', 'info');
+        } else {
+          // No live cases found for this roll number — drop demo matters too,
+          // keep only manually-added ones, and tell the user clearly.
+          setMattersState(localOnlyMatters);
+          LS.set('matters', localOnlyMatters);
+          LS.set('matters_lastSync', Date.now());
+          setLiveFetchStatus({
+            kind: 'no_cases',
+            message: `No cases found on the Allahabad HC CCMS for roll number "${lookupId}". Verify your Advocate Roll Number in Settings (format: B/A2401/2019), or add matters manually.`,
+            benchStatus: result?.benchStatus,
+            lookupId,
+          });
+          addToast('No cases found for your roll number. Check Settings to verify it.', 'info');
         }
       } catch (err) {
         console.error('[App] Live case fetch error:', err);
+        setLiveFetchStatus({ kind: 'error', message: err.message || 'Live fetch failed.' });
       } finally {
         if (!cancelled) setLoadingLiveCases(false);
       }
@@ -7762,7 +8228,7 @@ function App() {
     calendarTasks, setCalendarTasks,
     teamMembers, setTeamMembers,
     selectedMatter, setSelectedMatter, addToast, isOnline,
-    backendOnline, loadingLiveCases,
+    backendOnline, loadingLiveCases, liveFetchStatus,
   };
 
   const renderPage = () => {
